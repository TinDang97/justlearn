---
phase: 01-content-reading-shell
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/courses/[courseSlug]/layout.tsx
  - components/course-sidebar.tsx
  - components/mobile-sidebar-trigger.tsx
  - components/lesson-nav.tsx
  - components/lesson-breadcrumb.tsx
  - components/site-header.tsx
  - app/globals.css
  - __tests__/components/course-sidebar.test.tsx
  - __tests__/components/lesson-breadcrumb.test.tsx
autonomous: false
requirements:
  - LAYO-01
  - LAYO-02
  - LAYO-03
  - LAYO-04
  - LAYO-05
  - LAYO-06

must_haves:
  truths:
    - "Reading layout has Medium-inspired typography with 65-75 char line width and 1.75 line-height"
    - "Course sidebar shows all lessons with active lesson highlighted"
    - "Sidebar collapses to a slide-in Sheet drawer on mobile"
    - "Previous/next navigation appears at bottom of each lesson"
    - "Breadcrumb shows Home > Course > Lesson path"
    - "Dark/light mode toggle works with no white flash on page load"
    - "Layout has no horizontal overflow at 375px viewport width"
  artifacts:
    - path: "app/courses/[courseSlug]/layout.tsx"
      provides: "Course layout with sidebar + main content area"
      contains: "CourseSidebar"
    - path: "components/course-sidebar.tsx"
      provides: "Desktop sidebar with lesson list and active highlight"
      contains: "lessons.map"
    - path: "components/mobile-sidebar-trigger.tsx"
      provides: "Client component: Sheet drawer for mobile sidebar"
      contains: "use client"
    - path: "components/lesson-nav.tsx"
      provides: "Previous/next lesson navigation at bottom"
      contains: "prev"
    - path: "components/lesson-breadcrumb.tsx"
      provides: "Breadcrumb navigation component"
      contains: "BreadcrumbList"
    - path: "components/site-header.tsx"
      provides: "Global header with logo and theme toggle"
      contains: "ThemeToggle"
  key_links:
    - from: "app/courses/[courseSlug]/layout.tsx"
      to: "components/course-sidebar.tsx"
      via: "renders sidebar with lesson data from getCourse()"
      pattern: "CourseSidebar"
    - from: "components/mobile-sidebar-trigger.tsx"
      to: "components/ui/sheet.tsx"
      via: "shadcn Sheet for mobile drawer"
      pattern: "Sheet"
    - from: "components/lesson-nav.tsx"
      to: "lib/content.ts"
      via: "prev/next from LessonMeta"
      pattern: "prev.*next"
---

<objective>
Build the complete reading layout shell: site header, course sidebar (desktop + mobile), lesson breadcrumbs, previous/next navigation, Medium-quality typography, and mobile responsiveness at 375px.

Purpose: Transform raw MDX content pages into a polished, navigable reading experience that matches Medium's quality standard.
Output: Complete reading shell with all navigation components, responsive layout, and visual checkpoint for user sign-off.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-content-reading-shell/01-CONTEXT.md
@.planning/phases/01-content-reading-shell/01-RESEARCH.md
@.planning/phases/01-content-reading-shell/01-02-SUMMARY.md
</context>

<interfaces>
<!-- From lib/content.ts (Plan 01) -->
```typescript
export type LessonMeta = {
  slug: string; courseSlug: string; title: string;
  lessonNumber: number; duration: string; level: string;
  prev: string | null; next: string | null;
}
export type Course = {
  slug: string; title: string; description: string;
  level: string; lessonCount: number; lessons: LessonMeta[];
}
export function getAllCourses(): Course[];
export function getCourse(courseSlug: string): Course | undefined;
export function getLesson(courseSlug: string, lessonSlug: string): LessonMeta | undefined;
```

<!-- From shadcn/ui (Plan 01) -->
```typescript
// components/ui/sheet.tsx - Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle
// components/ui/breadcrumb.tsx - Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator
// components/ui/button.tsx - Button
// components/ui/separator.tsx - Separator
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build site header, course layout with sidebar, and all navigation components</name>
  <files>components/site-header.tsx, app/courses/[courseSlug]/layout.tsx, components/course-sidebar.tsx, components/mobile-sidebar-trigger.tsx, components/lesson-nav.tsx, components/lesson-breadcrumb.tsx, app/courses/[courseSlug]/[lessonSlug]/page.tsx, app/globals.css, __tests__/components/course-sidebar.test.tsx, __tests__/components/lesson-breadcrumb.test.tsx</files>
  <action>
1. Create components/site-header.tsx:
   - Sticky header (h-16, border-b, bg-background)
   - Left: site title/logo linking to /courses ("Python Lessons")
   - Right: ThemeToggle component (from Plan 01)
   - Full width, max-w-screen-2xl mx-auto, px-4

2. Update app/layout.tsx to include SiteHeader above the main content area.

3. Create components/course-sidebar.tsx as a Server Component per Research Pattern 7:
   - Desktop: aside with w-72, border-r, sticky top-16, h-[calc(100vh-4rem)], overflow-y-auto
   - hidden lg:block for desktop only
   - Render lesson list as nav links: lesson number + title
   - Active lesson gets bg-accent font-medium styling
   - IMPORTANT: Since this is a Server Component but needs current lessonSlug for highlighting, use a thin 'use client' wrapper or pass currentSlug from the layout. The recommended approach from Research is to use a Client Component wrapper reading usePathname() to derive the current lesson slug.

4. Create components/mobile-sidebar-trigger.tsx as 'use client':
   - Visible only on mobile (lg:hidden)
   - Menu icon button that opens shadcn Sheet from left side
   - Sheet contains the same lesson list navigation
   - Sheet closes on link click
   - Uses usePathname() to highlight current lesson

5. Create app/courses/[courseSlug]/layout.tsx per Research code example:
   - Server Component that gets courseSlug from params
   - Calls getCourse() -- notFound() if missing
   - Renders flex container: CourseSidebar + main content area
   - Main content: flex-1 min-w-0 (CRITICAL for preventing code block overflow)

6. Create components/lesson-breadcrumb.tsx per Research breadcrumb example:
   - Uses shadcn Breadcrumb components
   - Three levels: Courses (link to /courses) > Course Title (link to /courses/[slug]) > Lesson Title (current page)

7. Create components/lesson-nav.tsx as 'use client':
   - Previous/next lesson links at bottom of lesson page
   - Uses shadcn Button with ChevronLeft/ChevronRight icons
   - Shows lesson title in the link text
   - Null-safe: hides prev button on first lesson, next on last

8. Update app/courses/[courseSlug]/[lessonSlug]/page.tsx:
   - Add LessonBreadcrumb above the article
   - Add LessonNav below the article
   - Pass lesson metadata (prev, next, course title) to both components
   - Ensure article has Medium-quality prose: "prose prose-neutral dark:prose-invert max-w-[65ch] mx-auto px-4 py-8" with line-height 1.75

9. Update app/globals.css to ensure:
   - .prose line-height: 1.75
   - Code blocks: overflow-x: auto, max-width: 100%
   - No horizontal overflow at 375px (add min-w-0 to flex children)

10. Write __tests__/components/course-sidebar.test.tsx:
    - Renders correct number of lesson links
    - Highlights current lesson with correct class

11. Write __tests__/components/lesson-breadcrumb.test.tsx:
    - Renders three breadcrumb levels with correct text and links
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm build && pnpm test --run</automated>
  </verify>
  <done>Site header visible with theme toggle. Sidebar shows all lessons on desktop. Mobile sidebar opens as Sheet drawer. Breadcrumbs render correct path. Prev/next nav appears at lesson bottom. Typography matches Medium quality. Build and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete reading shell: course catalog, course pages, lesson pages with sidebar navigation, breadcrumbs, prev/next controls, dark/light mode, mobile responsive layout</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:3000
    2. Course catalog: verify 12 course cards display with titles, lesson counts, levels
    3. Click a course: verify lesson list shows all lessons with correct metadata
    4. Click a lesson: verify MDX renders with syntax-highlighted Python code
    5. Sidebar: verify all lessons listed, current lesson highlighted
    6. Breadcrumb: verify "Courses > Course Name > Lesson Title" appears above content
    7. Prev/next: scroll to bottom, verify previous/next controls with correct lesson titles
    8. Code blocks: verify copy button appears on hover, copies code to clipboard
    9. Dark mode: click theme toggle, verify no FOUC on page refresh in dark mode
    10. Mobile (375px): open Chrome DevTools, set viewport to 375px width:
        - Verify no horizontal overflow/scrollbar on the page
        - Verify sidebar is hidden, hamburger menu opens Sheet drawer
        - Verify code blocks scroll horizontally within their container
    11. Typography: verify reading area is approximately 65-75 characters wide with comfortable line spacing
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with all routes
- `pnpm test --run` passes all tests including sidebar and breadcrumb tests
- Visual verification of all LAYO-* requirements at desktop and 375px mobile
- Dark mode toggle works without FOUC
</verification>

<success_criteria>
- Site header with logo and theme toggle visible on all pages
- Course sidebar shows lessons with active highlight on desktop, Sheet drawer on mobile
- Breadcrumb shows correct 3-level navigation path
- Prev/next controls show at bottom of each lesson page
- Typography: 65-75ch line width, 1.75 line-height, clean minimalist aesthetic
- No horizontal overflow at 375px viewport
- Dark/light mode toggle with no FOUC
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-reading-shell/01-03-SUMMARY.md`
</output>
