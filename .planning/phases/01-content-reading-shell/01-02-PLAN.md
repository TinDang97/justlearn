---
phase: 01-content-reading-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/courses/page.tsx
  - app/courses/[courseSlug]/page.tsx
  - app/courses/[courseSlug]/[lessonSlug]/page.tsx
autonomous: true
requirements:
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "Course catalog page displays all 12 courses as cards with title, description, lesson count, and level"
    - "Each course page lists all its lessons with titles and descriptions"
    - "Each lesson page renders the full MDX content with syntax-highlighted Python code"
    - "All routes are statically generated via generateStaticParams"
  artifacts:
    - path: "app/courses/page.tsx"
      provides: "Course catalog page with 12 course cards"
      contains: "getAllCourses"
    - path: "app/courses/[courseSlug]/page.tsx"
      provides: "Course overview page listing all lessons"
      contains: "generateStaticParams"
    - path: "app/courses/[courseSlug]/[lessonSlug]/page.tsx"
      provides: "Lesson page rendering MDX content"
      contains: "generateStaticParams"
  key_links:
    - from: "app/courses/page.tsx"
      to: "lib/content.ts"
      via: "getAllCourses() call"
      pattern: "getAllCourses"
    - from: "app/courses/[courseSlug]/[lessonSlug]/page.tsx"
      to: "courses/"
      via: "dynamic import of .md files"
      pattern: "import.*courses.*\\.md"
---

<objective>
Create the three route pages: course catalog, course overview, and lesson content. All routes statically generated at build time using generateStaticParams with data from lib/content.ts.

Purpose: Students can browse courses and read any of the 122 lessons with syntax-highlighted code.
Output: Three working route pages that serve the complete course content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-content-reading-shell/01-CONTEXT.md
@.planning/phases/01-content-reading-shell/01-RESEARCH.md
@.planning/phases/01-content-reading-shell/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From lib/content.ts (created in Plan 01) -->
```typescript
export type LessonMeta = {
  slug: string;        // "lesson-01-what-is-programming"
  courseSlug: string;   // "01-python-fundamentals"
  title: string;       // "What is Programming?"
  lessonNumber: number; // 1
  duration: string;    // "2 hours"
  level: string;       // "Absolute Beginner"
  prev: string | null;
  next: string | null;
}

export type Course = {
  slug: string;        // "01-python-fundamentals"
  title: string;       // "Python Fundamentals"
  description: string;
  level: string;
  lessonCount: number;
  lessons: LessonMeta[];
}

export function getAllCourses(): Course[];
export function getCourse(courseSlug: string): Course | undefined;
export function getLesson(courseSlug: string, lessonSlug: string): LessonMeta | undefined;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create course catalog page and course overview page</name>
  <files>app/courses/page.tsx, app/courses/[courseSlug]/page.tsx</files>
  <action>
1. Create app/courses/page.tsx as a Server Component per Research code example:
   - Import getAllCourses from @/lib/content
   - Render a responsive grid (1 col mobile, 2 cols md, 3 cols lg) of shadcn Card components
   - Each card: Link wrapping Card with course.title in CardTitle, lesson count in Badge (variant="outline"), description in CardDescription (line-clamp-3), level in Badge (variant="secondary")
   - Add page metadata: title "Python Beginner Courses", description for SEO

2. Create app/courses/[courseSlug]/page.tsx as a Server Component:
   - Export generateStaticParams returning all courseSlug values from getAllCourses()
   - Export dynamicParams = false
   - Import getCourse, call with courseSlug from params (await params per Next.js 15 pattern)
   - If course not found, call notFound()
   - Render course title as h1, description paragraph, then a list of all lessons
   - Each lesson item: Link to /courses/[courseSlug]/[lessonSlug] showing lesson number, title, duration, and level badge
   - Add page metadata using generateMetadata

3. Update app/page.tsx to redirect to /courses (use redirect() from next/navigation or render the catalog inline).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm build 2>&1 | grep -E "(Route|error|Error)" | head -20</automated>
  </verify>
  <done>Course catalog shows 12 course cards. Each course page lists all its lessons. Build generates static pages for / and all 12 /courses/[slug] routes.</done>
</task>

<task type="auto">
  <name>Task 2: Create lesson page with MDX rendering and static generation</name>
  <files>app/courses/[courseSlug]/[lessonSlug]/page.tsx</files>
  <action>
1. Create app/courses/[courseSlug]/[lessonSlug]/page.tsx per Research Pattern 2:
   - Export generateStaticParams: flatMap all courses and lessons from getAllCourses()
   - Export dynamicParams = false
   - Await params (Next.js 15 async params pattern)
   - Dynamic import: const { default: LessonContent } = await import(`@/../courses/${courseSlug}/${lessonSlug}.md`)
   - CRITICAL: Test the import path. If @ alias points to project root, use `@/courses/...`. If it points elsewhere, adjust. Check tsconfig.json paths from Plan 01. If template literal dynamic imports from outside the app/ dir fail, try a relative path like `../../../../courses/${courseSlug}/${lessonSlug}.md` or create a symlink.
   - Wrap LessonContent in article with prose classes: "prose prose-neutral dark:prose-invert max-w-[65ch] mx-auto px-4 py-8"
   - Get lesson metadata via getLesson() for page title
   - Export generateMetadata for SEO (lesson title + course name)

2. Run pnpm build to verify ALL 122+ lesson routes generate successfully.
   - If build fails on specific lessons, check for edge cases in MD content (unusual characters, missing metadata).
   - Verify Course 05 generates all 12 lessons (not just 10).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>All 122+ lesson pages statically generated. pnpm build succeeds with no errors. Lesson content renders with Shiki syntax-highlighted Python code blocks.</done>
</task>

</tasks>

<verification>
- `pnpm build` generates static pages for all routes (12 course pages + 122 lesson pages + catalog)
- `pnpm dev` shows course catalog at /courses with 12 cards
- Clicking a course shows its lesson list
- Clicking a lesson renders MDX content with syntax-highlighted code
</verification>

<success_criteria>
- Course catalog displays all 12 courses as cards with correct metadata
- Each course page lists all its lessons with titles, durations, levels
- Each lesson page renders full MDX content with Shiki-highlighted Python code
- All routes are 100% statically generated (no server-side rendering)
- Copy-to-clipboard button appears on code blocks (wired via mdx-components.tsx from Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-reading-shell/01-02-SUMMARY.md`
</output>
