---
phase: 01-content-reading-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.mjs
  - tsconfig.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - mdx-components.tsx
  - lib/content.ts
  - components/copy-button.tsx
  - components/theme-provider.tsx
  - components/theme-toggle.tsx
  - vitest.config.ts
  - __tests__/lib/content.test.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-04

must_haves:
  truths:
    - "lib/content.ts parses all 12 course directories and extracts metadata from inline bold text (not YAML frontmatter)"
    - "MDX pipeline compiles .md files with Shiki syntax highlighting at build time"
    - "Copy button component renders on code blocks with clipboard API"
    - "Dark mode toggle works without FOUC via blocking script in head"
    - "Test suite runs and passes for content parsing logic"
  artifacts:
    - path: "lib/content.ts"
      provides: "Course and LessonMeta types, getAllCourses, getCourse, getLesson exports"
      exports: ["getAllCourses", "getCourse", "getLesson", "Course", "LessonMeta"]
    - path: "next.config.mjs"
      provides: "MDX + rehype-pretty-code + Shiki dual-theme config"
      contains: "createMDX"
    - path: "mdx-components.tsx"
      provides: "Global MDX overrides with copy-to-clipboard pre wrapper"
      contains: "useMDXComponents"
    - path: "components/copy-button.tsx"
      provides: "Client component for copy-to-clipboard on code blocks"
      contains: "use client"
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
    - path: "__tests__/lib/content.test.ts"
      provides: "Unit tests for content parsing"
  key_links:
    - from: "next.config.mjs"
      to: "mdx-components.tsx"
      via: "@next/mdx pipeline"
      pattern: "extension.*md"
    - from: "mdx-components.tsx"
      to: "components/copy-button.tsx"
      via: "pre override importing CopyButton"
      pattern: "CopyButton"
    - from: "lib/content.ts"
      to: "courses/"
      via: "fs.readFileSync at build time"
      pattern: "COURSES_DIR"
---

<objective>
Initialize the Next.js 15 project with App Router, install all Phase 1 dependencies, configure the MDX content pipeline with Shiki syntax highlighting, build the content parsing library, set up dark mode infrastructure, and establish the test framework.

Purpose: This is the foundation plan. Every subsequent plan depends on the typed data shapes from lib/content.ts and the MDX compilation pipeline configured here.
Output: Working Next.js project that can compile .md lesson files to syntax-highlighted pages, with unit tests proving content parsing works against all 12 course directories.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-reading-shell/01-CONTEXT.md
@.planning/phases/01-content-reading-shell/01-RESEARCH.md
@.planning/phases/01-content-reading-shell/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 project, install all dependencies, configure MDX pipeline</name>
  <files>package.json, pnpm-lock.yaml, next.config.mjs, tsconfig.json, app/layout.tsx, app/page.tsx, app/globals.css, mdx-components.tsx, components/theme-provider.tsx, components/theme-toggle.tsx, components/copy-button.tsx</files>
  <action>
1. Initialize Next.js 15 project with App Router (pnpm create next-app@latest . --typescript --tailwind --app --no-src-dir --eslint). If files already exist, work around them.

2. Install content pipeline deps:
   pnpm add @next/mdx @mdx-js/loader @mdx-js/react @types/mdx
   pnpm add rehype-pretty-code shiki remark-gfm rehype-slug unist-util-visit
   pnpm add @tailwindcss/typography

3. Install UI and theme deps:
   pnpm add next-themes @fontsource/inter

4. Initialize shadcn/ui (pnpm dlx shadcn@latest init) then add components:
   pnpm dlx shadcn@latest add button badge card breadcrumb sheet separator

5. Configure next.config.mjs per Research Pattern 3:
   - createMDX with extension: /\.(md|mdx)$/
   - remarkPlugins: [remarkGfm]
   - rehypePlugins: [extractRawCode, [rehypePrettyCode, { theme: { light: 'github-light', dark: 'github-dark-dimmed' }, keepBackground: false }], forwardRawCode, rehypeSlug]
   - pageExtensions: ['js', 'jsx', 'md', 'mdx', 'ts', 'tsx']
   - IMPORTANT: Do NOT use --turbopack flag. Remove it from dev script in package.json if present.

6. Create mdx-components.tsx at project root per Research Pattern 5:
   - Override `pre` to wrap with relative div + CopyButton
   - Override `code` to pass through without extra styling

7. Create components/copy-button.tsx as 'use client' component per Research Pattern 5:
   - navigator.clipboard.writeText on click
   - Toggle Copy/Check icons from lucide-react with 2s timeout
   - Hover-visible on desktop (opacity-0 group-hover:opacity-100), always visible on mobile

8. Create components/theme-provider.tsx wrapping next-themes ThemeProvider as 'use client'.

9. Create components/theme-toggle.tsx as 'use client' with Sun/Moon icon toggle using useTheme().

10. Configure app/layout.tsx per Research Pattern 4:
    - Import @fontsource/inter
    - suppressHydrationWarning on html tag
    - Blocking inline script in head reading localStorage theme
    - ThemeProvider wrapping body children with attribute="class" defaultTheme="system" enableSystem

11. Set app/page.tsx to a simple redirect or placeholder pointing to /courses.

12. Configure app/globals.css per Research Pattern 6:
    - Shiki dual-theme CSS variables (.shiki/.dark .shiki)
    - Medium-inspired prose overrides (line-height: 1.75)
    - Code block overflow-x: auto

13. Verify tsconfig.json paths alias @ points to project root so imports like @/lib/content work.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>Next.js project builds successfully with MDX pipeline configured, shadcn/ui initialized, dark mode infrastructure in place, copy-button component exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build lib/content.ts with inline metadata parsing and unit tests</name>
  <files>lib/content.ts, __tests__/lib/content.test.ts, vitest.config.ts</files>
  <behavior>
    - getAllCourses() returns exactly 12 Course objects sorted by directory name
    - Each Course has slug, title, description, level, lessonCount, and lessons array
    - parseLessonMeta extracts title from "# Lesson N: Title" pattern
    - parseLessonMeta extracts duration and level from "**Course:** X | **Duration:** Y | **Level:** Z" pattern
    - Lessons within a course are sorted by filename (lesson-01 before lesson-02)
    - prev/next links are null for first/last lessons respectively
    - Course 05 has 12 lessons (not 10) -- variable counts handled
    - getCourse returns undefined for non-existent slug
    - getLesson returns undefined for non-existent lesson slug
  </behavior>
  <action>
1. Install test framework:
   pnpm add -D vitest @vitejs/plugin-react @testing-library/react @testing-library/user-event jsdom

2. Create vitest.config.ts with:
   - @vitejs/plugin-react
   - test.environment: 'jsdom'
   - resolve.alias: @ -> project root path

3. Add "test" script to package.json: "vitest --run"

4. RED: Write __tests__/lib/content.test.ts testing all behaviors listed above. Tests read from the real courses/ directory (integration-style unit tests since data exists). Run tests -- they MUST fail (lib/content.ts doesn't exist yet).

5. GREEN: Create lib/content.ts per Research Pattern 1:
   - COURSES_DIR = path.join(process.cwd(), 'courses')
   - Types: LessonMeta (slug, courseSlug, title, lessonNumber, duration, level, prev, next) and Course (slug, title, description, level, lessonCount, lessons)
   - TITLE_REGEX, META_REGEX for inline bold text parsing (NOT gray-matter -- lesson files have NO YAML frontmatter)
   - parseLessonMeta(), getAllCourses(), getCourse(), getLesson() functions
   - CRITICAL: Parse course README.md for course title/description/level. Course title regex: /^#\s+Course\s+\d+:\s+(.+?)\s+-/m. Description regex: first paragraph after "## Course Description".

6. Run tests -- they MUST pass.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm test --run</automated>
  </verify>
  <done>getAllCourses returns 12 courses, each with correct metadata. getCourse/getLesson return correct data. All prev/next links correct. Tests green.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- `pnpm test --run` passes all content parsing tests
- lib/content.ts exports Course and LessonMeta types consumed by subsequent plans
- next.config.mjs processes .md files through Shiki pipeline
</verification>

<success_criteria>
- Next.js 15 project fully initialized with all Phase 1 dependencies
- MDX pipeline configured and compiles .md files with dual-theme syntax highlighting
- lib/content.ts parses all 12 courses and 122 lessons from courses/ directory
- Unit tests validate content parsing against real course data
- Dark mode infrastructure (theme-provider, theme-toggle, blocking script) in place
- Copy-to-clipboard component ready for MDX code blocks
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-reading-shell/01-01-SUMMARY.md`
</output>
