---
phase: 06-lesson-reading-toc-highlighting-practice
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - lib/content.ts
  - components/lesson-toc.tsx
  - components/scroll-progress.tsx
  - app/courses/[courseSlug]/[lessonSlug]/page.tsx
  - mdx-components.tsx
  - __tests__/lib/content.test.ts
  - __tests__/components/lesson-toc.test.tsx
  - __tests__/components/scroll-progress.test.tsx
  - __tests__/components/mdx-overrides.test.tsx
autonomous: true
requirements:
  - CHUNK-01
  - CHUNK-02
  - CHUNK-03
  - CHUNK-04

must_haves:
  truths:
    - "A sticky table of contents panel appears on desktop showing all lesson headings"
    - "The active heading is highlighted in the ToC as the student scrolls"
    - "On mobile, the ToC collapses to a compact bar below the lesson header"
    - "A thin progress bar at the top of the viewport fills as the student scrolls"
    - "Horizontal rule dividers visually separate lesson sections"
  artifacts:
    - path: "lib/content.ts"
      provides: "extractHeadings() function and Heading type"
      exports: ["extractHeadings", "Heading"]
    - path: "components/lesson-toc.tsx"
      provides: "Sticky desktop and collapsible mobile ToC with scroll spy"
      exports: ["LessonToc"]
    - path: "components/scroll-progress.tsx"
      provides: "Fixed progress bar at top of viewport"
      exports: ["ScrollProgress"]
    - path: "__tests__/lib/content.test.ts"
      provides: "Unit tests for extractHeadings"
    - path: "__tests__/components/lesson-toc.test.tsx"
      provides: "Unit tests for LessonToc with mocked IntersectionObserver"
    - path: "__tests__/components/scroll-progress.test.tsx"
      provides: "Unit tests for ScrollProgress RAF loop and cleanup"
    - path: "__tests__/components/mdx-overrides.test.tsx"
      provides: "Unit tests for hr divider override"
  key_links:
    - from: "lib/content.ts"
      to: "app/courses/[courseSlug]/[lessonSlug]/page.tsx"
      via: "extractHeadings called in Server Component"
      pattern: "extractHeadings"
    - from: "app/courses/[courseSlug]/[lessonSlug]/page.tsx"
      to: "components/lesson-toc.tsx"
      via: "headings prop passed to LessonToc"
      pattern: "LessonToc.*headings"
    - from: "components/lesson-toc.tsx"
      to: "rendered heading IDs"
      via: "IntersectionObserver observing heading elements by ID"
      pattern: "document.getElementById"
---

<objective>
Add in-page table of contents with scroll spy, scroll progress bar, and patch dividers to lesson pages.

Purpose: Students can orient themselves within long lessons via the ToC sidebar, see their scroll progress, and visually distinguish between lesson sections. This addresses the core "scroll fatigue" problem identified in the design spec.

Output: LessonToc component with desktop sticky sidebar and mobile collapsible bar, ScrollProgress bar, extractHeadings utility, hr divider override, and tests for all.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lesson-reading-toc-highlighting-practice/06-RESEARCH.md
@specs/v1.1-design-spec.md (sections 9, 14)

<interfaces>
<!-- Existing types from lib/content.ts -->
From lib/content.ts:
```typescript
export type LessonMeta = {
  slug: string;
  title: string;
  // ... other fields
}
export function getLesson(courseSlug: string, lessonSlug: string): LessonMeta | undefined
```

<!-- 06-01 creates these CSS tokens consumed by this plan -->
From app/globals.css (created by 06-01):
```css
--color-foreground, --color-foreground-muted, --color-foreground-subtle
--color-primary, --color-border, --color-background-subtle
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extractHeadings utility + LessonToc component + scroll spy + lesson page layout</name>
  <files>lib/content.ts, components/lesson-toc.tsx, app/courses/[courseSlug]/[lessonSlug]/page.tsx, __tests__/lib/content.test.ts, __tests__/components/lesson-toc.test.tsx</files>
  <behavior>
    - extractHeadings("## Hello\n### World") returns [{id: "hello", text: "Hello", level: 2}, {id: "world", text: "World", level: 3}]
    - extractHeadings handles headings with special chars: "## Part 1: What is Python? (30 minutes)" produces id "part-1-what-is-python-30-minutes"
    - extractHeadings ignores h1 and h4+ headings
    - extractHeadings returns empty array for content with no h2/h3
    - LessonToc renders a nav with heading links
    - LessonToc highlights the active heading
    - LessonToc shows compact mobile bar on small screens
  </behavior>
  <action>
1. Install `github-slugger` as a direct dependency: `pnpm add github-slugger`

2. In `lib/content.ts`, add after existing exports:
   ```typescript
   import GithubSlugger from 'github-slugger'
   export type Heading = { id: string; text: string; level: 2 | 3 }
   export function extractHeadings(mdContent: string): Heading[] {
     const slugger = new GithubSlugger()
     const regex = /^(#{2,3})\s+(.+)$/gm
     const headings: Heading[] = []
     let match
     while ((match = regex.exec(mdContent)) !== null) {
       const level = match[1].length as 2 | 3
       const text = match[2].trim()
       headings.push({ id: slugger.slug(text), text, level })
     }
     return headings
   }
   ```
   CRITICAL: Use `github-slugger`, NOT a custom slugify. This ensures IDs match `rehype-slug` output.

3. Add `extractHeadings` test cases to `__tests__/lib/content.test.ts`:
   - Basic h2 and h3 extraction
   - Special characters: parentheses, question marks, colons
   - Empty content returns empty array
   - h1 and h4 are ignored
   - Multiple headings produce correct sequential slugs

4. Create `components/lesson-toc.tsx` as a Client Component (`'use client'`):
   - Props: `{ headings: Heading[] }`
   - Implement `useActiveHeading(ids: string[])` hook using `IntersectionObserver` with `rootMargin: '-10% 0px -80% 0px'`
   - Desktop (xl+): vertical list of heading links, active item styled with `font-medium` and `--color-foreground` (others `--color-foreground-muted`), `text-sm` size
   - Mobile (<xl): `<details>`/`<summary>` compact 44px bar showing current heading text, expands to full list
   - Wrap in `<nav role="navigation" aria-label="Table of contents">`, active item gets `aria-current="true"`
   - Links are anchor `<a href={"#" + heading.id}>` elements

5. Create `__tests__/components/lesson-toc.test.tsx`:
   - Mock `IntersectionObserver` with `vi.stubGlobal`
   - Test heading links render with correct href
   - Test active heading gets aria-current
   - Test mobile compact bar renders

6. Update lesson page `app/courses/[courseSlug]/[lessonSlug]/page.tsx`:
   - Read raw MD file with `fs.readFileSync` and call `extractHeadings(rawMd)`
   - Switch outer layout from single `max-w-[65ch] mx-auto` to 2-column grid: `xl:grid xl:grid-cols-[65ch_240px] xl:gap-8 xl:max-w-[calc(65ch+240px+2rem)]`
   - Place `<LessonToc headings={headings} />` in a `<aside className="hidden xl:block">` with `sticky top-20`
   - On mobile (<xl), render `<LessonToc>` above the article content (visible as compact bar)
   - Add comment: `// TODO: When Phase 4 lands sourceCourseSlug on LessonMeta, use sourceCourseSlug for readFileSync path`
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm test -- __tests__/lib/content.test.ts __tests__/components/lesson-toc.test.tsx --reporter=verbose</automated>
  </verify>
  <done>extractHeadings returns correct Heading[] with github-slugger IDs. LessonToc renders heading links with scroll spy. Lesson page uses 2-column layout on xl+ with ToC sidebar. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scroll progress bar + hr patch divider override</name>
  <files>components/scroll-progress.tsx, mdx-components.tsx, __tests__/components/scroll-progress.test.tsx, __tests__/components/mdx-overrides.test.tsx, app/courses/[courseSlug]/[lessonSlug]/page.tsx</files>
  <behavior>
    - ScrollProgress renders a fixed div at top of viewport
    - ScrollProgress uses requestAnimationFrame (not CSS transition) for width updates
    - ScrollProgress cleanup cancels RAF on unmount
    - hr MDX override renders a centered thin line divider (not default hr)
  </behavior>
  <action>
1. Create `components/scroll-progress.tsx` as a Client Component:
   - Fixed position div at `top: 64px` (below site header), full width, 3px height
   - Background: `var(--color-primary)`
   - Width updated via `requestAnimationFrame` loop reading `document.documentElement.scrollTop / (scrollHeight - clientHeight)`
   - Use `useRef` for the bar element — set `style.width` directly (NOT setState)
   - `aria-hidden="true"` since it's decorative
   - Cleanup: `cancelAnimationFrame(rafId)` in useEffect cleanup to prevent leak on route navigation
   - Add `z-50` so it stays above content

2. Add `<ScrollProgress />` to the lesson page component (NOT root layout — lesson pages only). Place it as the first child inside the lesson page wrapper.

3. Create `__tests__/components/scroll-progress.test.tsx`:
   - Test component renders a div with fixed positioning
   - Test RAF cleanup is called on unmount (mock requestAnimationFrame / cancelAnimationFrame)
   - Test aria-hidden is set

4. Add `hr` override to `mdx-components.tsx`:
   ```typescript
   hr: () => (
     <div className="not-prose my-12 flex flex-col items-center gap-2">
       <div className="h-px w-20 bg-[var(--color-border)]" />
     </div>
   )
   ```
   This styles existing `---` in lesson markdown as centered thin dividers with generous spacing.

5. Create `__tests__/components/mdx-overrides.test.tsx`:
   - Test hr override renders the divider div with correct classes
   - Test divider has `not-prose` class
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm test -- __tests__/components/scroll-progress.test.tsx __tests__/components/mdx-overrides.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ScrollProgress renders fixed bar, updates via RAF, cleans up on unmount. hr override renders centered divider. Tests pass. Build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm test` — full suite passes
- `pnpm build` — build succeeds with new components and layout changes
- extractHeadings produces IDs matching rehype-slug output (verified by test cases with special characters)
- ScrollProgress cleanup prevents RAF leak (verified by test)
</verification>

<success_criteria>
1. extractHeadings correctly extracts h2/h3 headings with github-slugger IDs
2. LessonToc shows sticky sidebar on desktop and compact bar on mobile
3. Active heading highlighted via IntersectionObserver scroll spy
4. ScrollProgress bar fills as student scrolls through lesson
5. Lesson sections separated by styled hr dividers
6. All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-lesson-reading-toc-highlighting-practice/06-02-SUMMARY.md`
</output>
