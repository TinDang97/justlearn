---
phase: 06-lesson-reading-toc-highlighting-practice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - components/callout.tsx
  - mdx-components.tsx
  - __tests__/components/callout.test.tsx
autonomous: true
requirements:
  - READ-01
  - READ-02
  - READ-03
  - READ-04
  - READ-05

must_haves:
  truths:
    - "Lesson body text renders at 18px with 1.75 line-height on a warm off-white background"
    - "All code blocks and inline code use JetBrains Mono font"
    - "Blockquotes display with a green left border and green-tinted background, not italic"
    - "Tip, Warning, Info, and Error callout MDX components render with correct icon, color, and border"
  artifacts:
    - path: "app/globals.css"
      provides: "Warm palette CSS custom properties (light + dark), prose overrides, blockquote styling"
      contains: "--color-background"
    - path: "app/layout.tsx"
      provides: "JetBrains Mono font variable on html element"
      contains: "JetBrains_Mono"
    - path: "components/callout.tsx"
      provides: "Tip, Warning, Info, Error callout components"
      exports: ["Tip", "Warning", "Info", "Callout"]
    - path: "mdx-components.tsx"
      provides: "Callout components registered for MDX usage"
      contains: "Tip"
    - path: "__tests__/components/callout.test.tsx"
      provides: "Unit tests for callout component variants"
  key_links:
    - from: "components/callout.tsx"
      to: "mdx-components.tsx"
      via: "import and registration"
      pattern: "import.*callout"
    - from: "app/globals.css"
      to: "components/callout.tsx"
      via: "CSS custom properties consumed by Tailwind classes"
      pattern: "--color-primary"
---

<objective>
Establish the warm-neutral reading typography foundation for all lesson pages.

Purpose: This plan creates the CSS custom properties, font loading, prose overrides, blockquote styling, and callout MDX components that all subsequent Phase 6 plans depend on for their color values and type tokens.

Output: Updated globals.css with warm palette, JetBrains Mono loaded in layout.tsx, blockquote green-bordered callouts, and Tip/Warning/Info/Error MDX components with tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/v1.1-design-spec.md (sections 4, 5, 10, 12.6)
@.planning/phases/06-lesson-reading-toc-highlighting-practice/06-RESEARCH.md

<interfaces>
<!-- Existing exports the executor needs -->

From mdx-components.tsx:
```typescript
export function useMDXComponents(): MDXComponents {
  // ... existing pre, code overrides
}
```

From app/layout.tsx:
```typescript
// Currently loads Inter font. JetBrains_Mono must be added here.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Warm palette CSS tokens, prose overrides, JetBrains Mono font, blockquote styling</name>
  <files>app/globals.css, app/layout.tsx</files>
  <behavior>
    - Build succeeds with new CSS custom properties defined
    - JetBrains Mono font variable is applied to html element
  </behavior>
  <action>
1. In `app/globals.css`, add the full warm-neutral color palette as CSS custom properties in `:root` and `.dark` selectors. Use the exact values from design spec section 4:
   - Light mode: `--color-background: #FAFAF8`, `--color-background-subtle: #F5F5F0`, `--color-background-muted: #EEEDE8`, `--color-background-inset: #E8E7E2`, `--color-foreground: #1A1A19`, `--color-foreground-muted: #6B6B66`, `--color-foreground-subtle: #9C9C96`, `--color-primary: #16A34A`, `--color-primary-hover: #15803D`, `--color-primary-subtle: #DCFCE7`, `--color-primary-foreground: #FFFFFF`, `--color-border: #E5E5E0`, `--color-border-strong: #D4D4CF`, `--color-warning: #D97706`, `--color-error: #DC2626`, `--color-info: #2563EB`, `--color-code-bg: #F8F8F5`, `--color-code-border: #E5E5E0`, `--color-code-header: #F0F0EB`, `--color-accent: #2563EB`, `--color-accent-hover: #1D4ED8`
   - Dark mode: `--color-background: #1A1A1A`, `--color-background-subtle: #222222`, `--color-background-muted: #2A2A2A`, `--color-background-inset: #333333`, `--color-foreground: #EDEDEC`, `--color-foreground-muted: #A3A3A0`, `--color-foreground-subtle: #737370`, `--color-primary: #4ADE80`, `--color-primary-hover: #22C55E`, `--color-primary-subtle: rgba(74, 222, 128, 0.12)`, `--color-primary-foreground: #1A1A1A`, `--color-border: #333333`, `--color-border-strong: #444444`, `--color-code-bg: #1E1E1E`, `--color-code-border: #333333`, `--color-code-header: #252525`, `--color-accent: #60A5FA`, `--color-accent-hover: #3B82F6`

2. Map the existing shadcn/ui token names (`--background`, `--foreground`, `--primary`, etc.) to use the warm palette values (Option A from research — replace oklch values with warm palette equivalents). This ensures all shadcn components pick up the warm palette automatically.

3. Add prose overrides in `globals.css` per design spec section 10:
   - `.prose` font-size: 1.125rem, line-height: 1.75
   - `.prose` text/heading/link/quote color variables pointing to `--color-*` tokens
   - `.prose h2`: font-size 1.5rem, margin-top 3rem, margin-bottom 1rem, font-weight 600, scroll-margin-top 5rem
   - `.prose h3`: font-size 1.25rem, margin-top 2rem, margin-bottom 0.75rem, font-weight 600
   - `.prose p`: margin-bottom 1.5rem
   - `.prose blockquote`: border-left 3px solid `var(--color-primary)`, background `var(--color-primary-subtle)`, padding 1rem 1.5rem, border-radius 0 8px 8px 0, font-style normal, color `var(--color-foreground)`
   - `.prose code:not(pre code)`: background `var(--color-background-muted)`, padding 0.125rem 0.375rem, border-radius 6px, font-family `var(--font-mono)`, font-size 0.875rem, font-weight 500
   - `.prose a`: color `var(--color-accent)`, text-decoration underline, text-underline-offset 3px
   - `.prose a:hover`: color `var(--color-accent-hover)`

4. In `app/layout.tsx`, import `JetBrains_Mono` from `next/font/google`:
   ```typescript
   import { JetBrains_Mono } from 'next/font/google'
   const jetbrainsMono = JetBrains_Mono({
     subsets: ['latin'],
     variable: '--font-mono',
     display: 'swap',
   })
   ```
   Add `jetbrainsMono.variable` to the `<html>` element className.

5. Apply `scroll-behavior: smooth` scoped to lesson content area (not globally — add it to `.prose` or a `.lesson-content` wrapper class).

IMPORTANT: Do NOT add `--turbo` to any npm scripts. Stay on Webpack.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm build</automated>
  </verify>
  <done>Build succeeds. globals.css contains all warm palette tokens for light and dark mode. JetBrains Mono variable class applied to html element. Prose overrides set 18px body, 1.75 line-height, green-bordered blockquotes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Callout MDX components (Tip, Warning, Info, Error) with tests</name>
  <files>components/callout.tsx, mdx-components.tsx, __tests__/components/callout.test.tsx</files>
  <behavior>
    - Callout renders with correct variant-specific icon, border color, and background
    - Tip variant shows Lightbulb icon with green border
    - Warning variant shows AlertTriangle icon with amber border
    - Info variant shows Info icon with blue border
    - Error variant shows AlertCircle icon with red border
    - Custom title overrides default variant title
    - Children render inside callout body
  </behavior>
  <action>
1. Create `components/callout.tsx` with a `Callout` base component and four shorthand exports: `Tip`, `Warning`, `Info`, `ErrorCallout`. Use the exact pattern from research Pattern 6:
   - Props: `{ variant: 'tip' | 'warning' | 'info' | 'error', title?: string, children: ReactNode }`
   - Each variant has: icon (from lucide-react), border class, bg class, default title
   - Variant config: tip = Lightbulb/primary/green-50, warning = AlertTriangle/warning/amber-50, info = Info/info/blue-50, error = AlertCircle/error/red-50
   - Use `not-prose` class to escape Tailwind typography defaults
   - Left border 3px using `border-l-[3px]`

2. Register in `mdx-components.tsx`:
   ```typescript
   import { Tip, Warning, Info, ErrorCallout } from '@/components/callout'
   // In the return object:
   Tip,
   Warning,
   Info,
   Error: ErrorCallout,  // Avoid shadowing global Error constructor
   ```

3. Create `__tests__/components/callout.test.tsx`:
   - Test each variant renders with correct role/structure
   - Test default title matches variant name
   - Test custom title overrides default
   - Test children content renders
   - Test shorthand components (Tip, Warning, Info, ErrorCallout) map to correct variant
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/lessons/python-beginer && pnpm test -- __tests__/components/callout.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Callout component exports Tip/Warning/Info/ErrorCallout. All 4 variants render with correct icon, border, background. Registered in mdx-components.tsx. Tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds (no CSS or import errors)
- `pnpm test -- __tests__/components/callout.test.tsx` passes
- globals.css contains `--color-background`, `--color-primary`, `--color-code-bg` in both `:root` and `.dark`
- layout.tsx imports and applies JetBrains_Mono variable
</verification>

<success_criteria>
1. Warm palette CSS tokens defined for both light and dark mode
2. Prose overrides produce 18px/1.75 body text with green-bordered blockquotes
3. JetBrains Mono loaded and applied via --font-mono CSS variable
4. Callout components (Tip, Warning, Info, Error) render correctly with distinct styling
5. All callout tests pass
6. Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-lesson-reading-toc-highlighting-practice/06-01-SUMMARY.md`
</output>
