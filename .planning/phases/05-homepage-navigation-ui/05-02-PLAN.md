---
phase: 05-homepage-navigation-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/courses/[courseSlug]/page.tsx
  - app/courses/[courseSlug]/layout.tsx
  - app/courses/[courseSlug]/[lessonSlug]/page.tsx
  - components/course-sidebar.tsx
  - components/mobile-sidebar-trigger.tsx
  - components/lesson-breadcrumb.tsx
  - __tests__/components/course-overview-accordion.test.tsx
  - __tests__/components/course-sidebar.test.tsx
  - __tests__/components/lesson-breadcrumb.test.tsx
autonomous: true
requirements:
  - STRUCT-02
  - STRUCT-03
  - STRUCT-05

must_haves:
  truths:
    - "Course overview page at /courses/python shows 12 sections as an accordion with per-section lesson count and progress"
    - "First incomplete section is expanded by default in the accordion"
    - "Lesson sidebar groups lessons under section headers with expand/collapse controls"
    - "The section containing the current lesson auto-expands in the sidebar"
    - "Mobile sidebar sheet shows the same section-grouped layout as desktop"
    - "Breadcrumb reads Python Course > Section Name > Lesson Title on every lesson page"
  artifacts:
    - path: "app/courses/[courseSlug]/page.tsx"
      provides: "Course overview with Accordion sections showing lesson lists and per-section progress"
      min_lines: 50
    - path: "app/courses/[courseSlug]/layout.tsx"
      provides: "Layout passing sections: Section[] to sidebar and mobile trigger"
      min_lines: 15
    - path: "components/course-sidebar.tsx"
      provides: "Section-grouped sidebar with Collapsible per section, active section auto-expanded"
      min_lines: 50
    - path: "components/mobile-sidebar-trigger.tsx"
      provides: "Mobile sheet with same section-grouped lesson list"
      min_lines: 50
    - path: "components/lesson-breadcrumb.tsx"
      provides: "4-level breadcrumb: Python Course > Section > Lesson"
      min_lines: 25
    - path: "app/courses/[courseSlug]/[lessonSlug]/page.tsx"
      provides: "Lesson page passing sectionSlug and sectionTitle props to LessonBreadcrumb"
      min_lines: 10
  key_links:
    - from: "app/courses/[courseSlug]/layout.tsx"
      to: "lib/content.ts"
      via: "getUnifiedCourse() call to get sections"
      pattern: "getUnifiedCourse"
    - from: "components/course-sidebar.tsx"
      to: "radix-ui"
      via: "Collapsible.Root/Trigger/Content for section grouping"
      pattern: "Collapsible"
    - from: "app/courses/[courseSlug]/page.tsx"
      to: "radix-ui"
      via: "Accordion.Root/Item/Trigger/Content for section list"
      pattern: "Accordion"
    - from: "components/lesson-breadcrumb.tsx"
      to: "app/courses/[courseSlug]/[lessonSlug]/page.tsx"
      via: "sectionTitle and sectionSlug props passed from lesson page"
      pattern: "sectionTitle.*sectionSlug"
    - from: "app/courses/[courseSlug]/[lessonSlug]/page.tsx"
      to: "components/lesson-breadcrumb.tsx"
      via: "Passes sectionSlug and sectionTitle props derived from getUnifiedCourse().sections"
      pattern: "sectionSlug.*sectionTitle"
---

<objective>
Build the course overview accordion, section-grouped sidebar, and updated breadcrumbs for the unified Python Course.

Purpose: Students navigate the unified course through a structured accordion overview, a collapsible section-grouped sidebar, and breadcrumbs reflecting the Course > Section > Lesson hierarchy.
Output: Accordion course overview at /courses/python, section-grouped sidebar (desktop + mobile), 4-level breadcrumbs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-homepage-navigation-ui/05-RESEARCH.md
@specs/v1.1-design-spec.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/content.ts (Phase 4 deliverables — must exist before this plan runs):
```typescript
export type LessonMeta = {
  slug: string
  title: string
  lessonNumber: number
  duration: string
  level: string
  sectionSlug: string       // NEW in Phase 4
  sourceCourseSlug: string  // NEW in Phase 4
}

export type Section = {
  slug: string
  title: string
  description: string
  lessonCount: number
  lessons: LessonMeta[]
}

export type UnifiedCourse = {
  slug: string          // 'python'
  title: string
  description: string
  sections: Section[]
  allLessons: LessonMeta[]
}

export function getUnifiedCourse(): UnifiedCourse
```

From components/course-sidebar.tsx (current — will be rewritten):
```typescript
type CourseSidebarProps = {
  courseSlug: string
  lessons: LessonMeta[]
}
// Changes to: sections: Section[] instead of lessons: LessonMeta[]
```

From components/mobile-sidebar-trigger.tsx (current — will be rewritten):
```typescript
type MobileSidebarTriggerProps = {
  courseSlug: string
  lessons: LessonMeta[]
  courseTitle: string
}
// Changes to: sections: Section[] instead of lessons: LessonMeta[]
```

From components/lesson-breadcrumb.tsx (current — will be updated):
```typescript
type LessonBreadcrumbProps = {
  courseSlug: string
  courseTitle: string
  lessonTitle: string
}
// Adds: sectionSlug: string, sectionTitle: string
```

From radix-ui (already installed v1.4.3):
```typescript
import { Accordion } from 'radix-ui'
// Accordion.Root, Accordion.Item, Accordion.Trigger, Accordion.Content

import { Collapsible } from 'radix-ui'
// Collapsible.Root, Collapsible.Trigger, Collapsible.Content
```

From lib/store/progress.ts:
```typescript
// useProgressStore.isComplete(courseSlug, lessonSlug) — courseSlug is 'python' post-Phase 4
```

From components/ui/breadcrumb.tsx:
```typescript
export { Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Course overview accordion + layout update</name>
  <files>app/courses/[courseSlug]/page.tsx, app/courses/[courseSlug]/layout.tsx, __tests__/components/course-overview-accordion.test.tsx</files>
  <behavior>
    - Course overview renders all 12 section titles as accordion items
    - Each accordion item shows section number, title, lesson count, and progress fraction (e.g., "3/10")
    - Expanding an accordion item reveals the lesson list with links
    - Each lesson link href follows /courses/python/{lessonSlug} pattern
    - First incomplete section is expanded by default
    - Completed sections show a green checkmark badge
    - Layout passes sections: Section[] to CourseSidebar and MobileSidebarTrigger
  </behavior>
  <action>
    1. Create `__tests__/components/course-overview-accordion.test.tsx`:
       - Mock `getUnifiedCourse` to return a course with 3 sections (2-3 lessons each)
       - Mock `useProgressStore` for progress data
       - Test: all section titles rendered
       - Test: lesson count per section shown
       - Test: expanding a section shows lesson titles
       - Test: lesson links have correct hrefs

    2. Rewrite `app/courses/[courseSlug]/page.tsx`:
       - Import `getUnifiedCourse` from @/lib/content (replaces `getCourse`)
       - Import `{ Accordion }` from 'radix-ui'
       - Keep `export const dynamicParams = false`
       - Update `generateStaticParams` to return `[{ courseSlug: 'python' }]`
       - Update `generateMetadata` to use `getUnifiedCourse()`
       - The page is a Server Component but uses Accordion which is inherently client-side. Create a client sub-component `CourseOverviewAccordion` inline or as a separate export within the same file (or a new file). The simplest approach: make the page a Server Component that renders a client wrapper.
       - Actually, create the accordion as a 'use client' component within the page file or extract to a small client component. The server component fetches data and passes it down.
       - Structure:
         ```
         <main className="max-w-3xl mx-auto px-4 py-10">
           <h1>Python -- From Zero to Confident</h1>
           <p>{description}</p>
           <CourseProgressBar courseSlug="python" totalLessons={course.allLessons.length} />
           <CourseOverviewAccordion sections={course.sections} courseSlug="python" />
         </main>
         ```
       - `CourseOverviewAccordion` ('use client'): Uses `Accordion.Root` with `type="single" collapsible`. Default value = slug of first incomplete section (determined via `useProgressStore`). Each `Accordion.Item` has:
         - Trigger: section number (zero-padded), section title, lesson count badge, progress text "X/Y", chevron icon
         - Content: ordered list of lessons with completion icon (CheckCircle2/Circle), lesson number, title, duration. Each is a Link to `/courses/python/${lesson.slug}`
         - Completed sections: green CheckCircle2 icon next to title in trigger
       - Keep the `NotebookLMCard` if appropriate, or remove it since it was per-course and now we have a unified course (keep it — it still works with courseSlug='python' via the sourceCourseSlug mapping)

    3. Update `app/courses/[courseSlug]/layout.tsx`:
       - Import `getUnifiedCourse` instead of `getCourse`
       - Call `const course = getUnifiedCourse()` (no courseSlug param needed — it always returns the unified course)
       - Pass `sections={course.sections}` to `CourseSidebar` instead of `lessons={course.lessons}`
       - Pass `sections={course.sections}` to `MobileSidebarTrigger` instead of `lessons={course.lessons}`
       - Keep courseSlug from params for the URL context
       - Note: This will cause TypeScript errors on CourseSidebar and MobileSidebarTrigger until Task 2 updates their prop interfaces. If executing Task 1 first, the build will fail on the old prop types. To handle this: either update the layout last (after Task 2), or update the sidebar/mobile prop types at the start of Task 2. The executor should update layout.tsx AFTER Task 2's sidebar changes, or update both tasks atomically. Since tasks run sequentially within a plan, update layout in this task but acknowledge the TS error will be fixed in Task 2.
       - Actually, to avoid a broken intermediate state: stub the new prop interface on CourseSidebar and MobileSidebarTrigger in this task (just change the type, let the body be a TODO that Task 2 fills in). No — that breaks the rule against stubs. Better approach: update layout.tsx in Task 2 alongside the sidebar changes. Move layout.tsx to Task 2.

    4. Run tests: `pnpm test -- __tests__/components/course-overview-accordion.test.tsx --run`
  </action>
  <verify>
    <automated>pnpm test -- __tests__/components/course-overview-accordion.test.tsx --run</automated>
  </verify>
  <done>Course overview page renders accordion with all sections. Each section shows title, lesson count, progress. Expanding shows lesson list with links. First incomplete section expanded by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Section-grouped sidebar (desktop + mobile) + breadcrumb update + layout wiring + lesson page call-site</name>
  <files>components/course-sidebar.tsx, components/mobile-sidebar-trigger.tsx, components/lesson-breadcrumb.tsx, app/courses/[courseSlug]/layout.tsx, app/courses/[courseSlug]/[lessonSlug]/page.tsx, __tests__/components/course-sidebar.test.tsx, __tests__/components/lesson-breadcrumb.test.tsx</files>
  <behavior>
    - CourseSidebar accepts sections: Section[] prop (not lessons: LessonMeta[])
    - Sidebar renders section headers with expand/collapse chevron
    - The section containing the current lesson is auto-expanded on mount
    - Other sections are collapsed by default
    - Clicking a section header toggles its lesson list visibility
    - Each lesson link shows completion icon and lesson number
    - MobileSidebarTrigger has identical section-grouped behavior inside the Sheet
    - Breadcrumb renders 3 items: Python Course > Section Name > Lesson Title
    - Breadcrumb Python Course links to /courses/python
    - Breadcrumb Section Name links to /courses/python#sectionSlug
    - Layout passes sections to both sidebar components
    - Lesson page resolves the section for the current lesson and passes sectionSlug + sectionTitle to LessonBreadcrumb
  </behavior>
  <action>
    1. Update `__tests__/components/course-sidebar.test.tsx`:
       - Change test data from `lessons: LessonMeta[]` to `sections: Section[]` with 2-3 sections, 2-3 lessons each
       - Mock usePathname to return a specific lesson path
       - Mock useProgressStore
       - Test: section headers rendered for each section
       - Test: section containing active lesson is expanded (lesson links visible)
       - Test: other sections are collapsed (lesson links not visible)
       - Test: clicking a collapsed section header expands it

    2. Update `__tests__/components/lesson-breadcrumb.test.tsx`:
       - Add sectionSlug and sectionTitle to test props
       - Test: renders 3 breadcrumb links (Python Course, Section, Lesson)
       - Test: Python Course links to /courses/python
       - Test: Section links to /courses/python#sectionSlug
       - Test: Lesson title is the current page (not a link)

    3. Rewrite `components/course-sidebar.tsx`:
       - Keep 'use client'
       - Import `{ Collapsible }` from 'radix-ui', `ChevronDown` from lucide-react
       - Change props to `{ courseSlug: string, sections: Section[] }`
       - Import Section type from @/lib/content
       - For each section, render a `Collapsible.Root`:
         - `open` state: determined by whether the section contains the current lesson (match pathname). Use `useState` initialized per section. The section whose lessons include the current pathname slug starts open; others start closed.
         - `Collapsible.Trigger`: flex row with section title (text-xs font-semibold uppercase tracking-wider text-muted-foreground), ChevronDown icon (rotate-180 when open, transition-transform)
         - `Collapsible.Content`: ul of lesson links (same styling as current: flex items-start gap-3, rounded-md, completion icons, lesson number, title)
       - Keep existing lesson link styling and completion icon logic
       - Use the `usePathname` hook (already imported) to determine active lesson

    4. Rewrite `components/mobile-sidebar-trigger.tsx`:
       - Change props to `{ courseSlug: string, sections: Section[], courseTitle: string }`
       - Apply identical section-grouped rendering inside the Sheet content
       - Each section gets a Collapsible with same open/closed logic as desktop
       - Keep the Sheet open/close behavior and onClick={() => setOpen(false)} on links

    5. Update `app/courses/[courseSlug]/layout.tsx`:
       - Import `getUnifiedCourse` from @/lib/content (replace `getCourse`)
       - Call `const course = getUnifiedCourse()`
       - Pass `sections={course.sections}` to CourseSidebar (replaces `lessons={course.lessons}`)
       - Pass `sections={course.sections}` to MobileSidebarTrigger (replaces `lessons={course.lessons}`)
       - Keep courseSlug from params for URL routing context
       - Update courseTitle to `course.title`

    6. Update `components/lesson-breadcrumb.tsx`:
       - Add `sectionSlug: string` and `sectionTitle: string` to props type
       - Change the breadcrumb from 3 levels (Courses > Course > Lesson) to 3 items:
         - Item 1: BreadcrumbLink "Python Course" href={`/courses/${courseSlug}`}
         - Item 2: BreadcrumbLink `{sectionTitle}` href={`/courses/${courseSlug}#${sectionSlug}`}
         - Item 3: BreadcrumbPage `{lessonTitle}`
       - Remove the old "Courses" top-level link (there is no /courses catalog page anymore)

    7. Update `app/courses/[courseSlug]/[lessonSlug]/page.tsx` (the LessonBreadcrumb call site):
       - Import `getUnifiedCourse` from @/lib/content (if not already imported)
       - After retrieving the current lesson, resolve its parent section: call `const course = getUnifiedCourse()` then `const section = course.sections.find(s => s.lessons.some(l => l.slug === lessonSlug))` (where lessonSlug comes from params)
       - Pass the two new required props to `<LessonBreadcrumb />`: `sectionSlug={section?.slug ?? ''}` and `sectionTitle={section?.title ?? ''}`
       - This ensures the TypeScript build does not fail with missing-prop errors on the updated LessonBreadcrumbProps interface

    8. Run full test suite: `pnpm test -- --run`
  </action>
  <verify>
    <automated>pnpm test -- __tests__/components/course-sidebar.test.tsx __tests__/components/lesson-breadcrumb.test.tsx --run</automated>
  </verify>
  <done>Sidebar groups lessons under section headers with Collapsible expand/collapse. Active section auto-expands. Mobile sheet has identical grouping. Breadcrumb shows Python Course > Section > Lesson with correct hrefs. Layout passes sections to both sidebar components. Lesson page passes sectionSlug and sectionTitle to LessonBreadcrumb. All tests green.</done>
</task>

</tasks>

<verification>
- `pnpm test -- --run` — full test suite green (including updated course-sidebar and lesson-breadcrumb tests)
- `pnpm build` — build completes without errors
- Visit `/courses/python` — accordion shows 12 sections, first incomplete expanded, lessons listed when expanded
- Click sidebar section headers — sections expand/collapse, active lesson's section is open
- Mobile: open sidebar sheet — same section grouping visible
- Breadcrumb on lesson page — reads "Python Course > Section Name > Lesson Title"
</verification>

<success_criteria>
1. Course overview at /courses/python shows 12 accordion sections with progress
2. Expanding a section reveals its lesson list with links and completion icons
3. Sidebar groups lessons under collapsible section headers
4. Active lesson's section auto-expands in sidebar
5. Mobile sidebar sheet has identical section grouping
6. Breadcrumbs read "Python Course > Section Name > Lesson Title" on every lesson page
7. Lesson page passes sectionSlug and sectionTitle to LessonBreadcrumb without TypeScript errors
8. All new and updated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-homepage-navigation-ui/05-02-SUMMARY.md`
</output>
