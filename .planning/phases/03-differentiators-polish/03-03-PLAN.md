---
phase: 03-differentiators-polish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/notebook-urls.ts
  - components/notebook-lm/NotebookLMCard.tsx
  - components/lesson-complete-button.tsx
  - components/code-runner/code-runner-client.tsx
  - app/courses/[courseSlug]/page.tsx
  - app/template.tsx
  - __tests__/components/notebook-lm-card.test.tsx
  - __tests__/components/template.test.tsx
  - __tests__/components/micro-animations.test.tsx
autonomous: false
requirements: [NBLM-01, NBLM-02, NBLM-03, ANIM-01, ANIM-02]

user_setup:
  - service: notebooklm
    why: "Pre-seeded AI Q&A notebooks for each course"
    env_vars: []
    dashboard_config:
      - task: "Create 12 NotebookLM notebooks (one per course)"
        location: "https://notebooklm.google.com — requires personal Google account"
        steps:
          - "Create a notebook for each of the 12 courses"
          - "Upload the course's lesson .md files as sources"
          - "Enable public sharing on each notebook"
          - "Copy the public share URL for each notebook"
          - "Update lib/notebook-urls.ts with the 12 URLs"

must_haves:
  truths:
    - "Each course page shows a NotebookLM card with explanation text and a link to the AI Q&A notebook"
    - "NotebookLM card clearly explains what the tool is and that a Google account is needed"
    - "Page transitions between routes are smooth fade-in animations"
    - "Interactive elements have micro-animation feedback on hover and tap"
    - "LessonCompleteButton and Run button have motion whileHover/whileTap animations"
  artifacts:
    - path: "lib/notebook-urls.ts"
      provides: "Static mapping of courseSlug to NotebookLM public share URLs"
      exports: ["NOTEBOOK_URLS"]
    - path: "components/notebook-lm/NotebookLMCard.tsx"
      provides: "Server Component card with explanation text and external link"
    - path: "app/template.tsx"
      provides: "Motion page transition wrapper for App Router"
    - path: "app/courses/[courseSlug]/page.tsx"
      provides: "Course page with NotebookLMCard added"
    - path: "components/lesson-complete-button.tsx"
      provides: "Mark Complete button with motion whileHover/whileTap"
    - path: "components/code-runner/code-runner-client.tsx"
      provides: "Run button with motion whileHover/whileTap"
  key_links:
    - from: "components/notebook-lm/NotebookLMCard.tsx"
      to: "lib/notebook-urls.ts"
      via: "import NOTEBOOK_URLS"
      pattern: "NOTEBOOK_URLS"
    - from: "app/courses/[courseSlug]/page.tsx"
      to: "components/notebook-lm/NotebookLMCard.tsx"
      via: "import and render NotebookLMCard"
      pattern: "NotebookLMCard"
    - from: "app/template.tsx"
      to: "motion/react"
      via: "motion.div wrapper"
      pattern: "motion\\.div"
    - from: "components/lesson-complete-button.tsx"
      to: "motion/react"
      via: "motion.button with whileHover/whileTap"
      pattern: "motion\\.button"
    - from: "components/code-runner/code-runner-client.tsx"
      to: "motion/react"
      via: "motion.button with whileHover/whileTap on Run button"
      pattern: "motion\\.button"
---

<objective>
NotebookLM integration cards on course pages, smooth page transition animations, and micro-animation polish across the platform.

Purpose: Complete the v1 product experience with AI-powered Q&A links per course, polished page transitions, and responsive micro-animations. NotebookLM gives students an AI study assistant grounded in actual course content. Page transitions and micro-animations signal a polished, professional product.

Output: NotebookLMCard component on all 12 course pages, Motion page transitions via template.tsx, motion micro-animations on LessonCompleteButton and Run button, CSS hover on lesson cards.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiators-polish/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts from the existing codebase -->

From app/courses/[courseSlug]/page.tsx (will be modified):
```typescript
// Server Component — renders course overview with lesson list
// Currently renders: back link, badge/lesson count, title, description, CourseProgressBar, lesson list
// Exports: generateStaticParams, generateMetadata, default CoursePage
// Props: { params: Promise<{ courseSlug: string }> }
```

From app/layout.tsx (NOT modified — template.tsx is a sibling):
```typescript
// Root layout: html > body > ThemeProvider > ProgressHydration > SiteHeader > {children}
// template.tsx wraps {children} at the same level as layout.tsx
```

From components/site-header.tsx:
```typescript
// Sticky header h-16, contains logo Link and ThemeToggle
// (Plan 02 adds SearchDialog here — no conflict, different files)
```

From components/lesson-complete-button.tsx (will be modified for ANIM-02):
```typescript
'use client'
import { Button } from '@/components/ui/button'
// Renders two Button variants: "Mark Complete" (default) and "Completed" (outline, green)
// Both are shadcn <Button> elements — replace inner <Button> rendering with motion.button
```

From components/code-runner/code-runner-client.tsx (will be modified for ANIM-02):
```typescript
'use client'
import { Button } from '@/components/ui/button'
// Renders a Run <Button size="sm"> — replace with motion.button wrapper
```

Existing interactive elements that will receive micro-animations:
- components/lesson-complete-button.tsx (Button) — motion.button with whileHover/whileTap
- components/code-runner/code-runner-client.tsx (Run button) — motion.button with whileHover/whileTap
- app/courses/[courseSlug]/page.tsx (lesson list cards with hover:bg-muted/50) — CSS transition-transform
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotebookLM card, page transitions, and micro-animations</name>
  <files>
    lib/notebook-urls.ts,
    components/notebook-lm/NotebookLMCard.tsx,
    components/lesson-complete-button.tsx,
    components/code-runner/code-runner-client.tsx,
    app/courses/[courseSlug]/page.tsx,
    app/template.tsx,
    __tests__/components/notebook-lm-card.test.tsx,
    __tests__/components/template.test.tsx,
    __tests__/components/micro-animations.test.tsx
  </files>
  <behavior>
    - Test: NotebookLMCard renders explanation text about NotebookLM (NBLM-02)
    - Test: NotebookLMCard renders an external link with correct href for a known courseSlug (NBLM-01)
    - Test: NotebookLMCard renders "Google account required" notice
    - Test: NotebookLMCard returns null when courseSlug has no URL mapping
    - Test: template.tsx renders children wrapped in a motion.div element (ANIM-01)
    - Test: LessonCompleteButton renders a motion.button element with whileHover prop (ANIM-02)
    - Test: CodeRunnerClient Run button renders a motion.button element with whileTap prop (ANIM-02)
  </behavior>
  <action>
    1. Create `lib/notebook-urls.ts`:
       - Export `NOTEBOOK_URLS: Record<string, string>` with placeholder URLs for all 12 courses
       - Use the actual course slugs: '01-python-fundamentals', '02-data-types-variables', '03-control-flow-logic', '04-functions-modules', '05-data-structures', '06-oop', '07-file-handling-exceptions', '08-working-with-libraries', '09-web-development-basics', '10-data-analysis-visualization', '11-automation-scripting', '12-capstone-best-practices'
       - Placeholder URLs: `'https://notebooklm.google.com/notebook/PLACEHOLDER_{slug}'`
       - Add a comment: "Replace PLACEHOLDER URLs with actual NotebookLM public share URLs after creating notebooks"

    2. Create `components/notebook-lm/NotebookLMCard.tsx`:
       - This is a Server Component (no 'use client' needed — purely static rendering)
       - Import NOTEBOOK_URLS from lib/notebook-urls
       - Import { ExternalLink } from 'lucide-react'
       - Props: { courseSlug: string }
       - If no URL for courseSlug, return null
       - Render a card with:
         - Border, rounded-lg, p-6, bg-card styling
         - Icon/emoji header: graduation cap or brain icon
         - h3: "AI Study Assistant"
         - Explanation paragraph: "Ask questions about this course in plain language. NotebookLM is a free Google AI tool that answers based on the actual course materials — not the wider internet. It can summarize lessons, explain concepts, and quiz you on what you've learned."
         - Notice: "You'll need a Google account to access it." in text-muted-foreground text-sm
         - External link button: `<a href={url} target="_blank" rel="noopener noreferrer">` styled as a primary button with ExternalLink icon
         - Button text: "Open in NotebookLM"

    3. Modify `app/courses/[courseSlug]/page.tsx`:
       - Import NotebookLMCard from '@/components/notebook-lm/NotebookLMCard'
       - Add `<NotebookLMCard courseSlug={courseSlug} />` after the CourseProgressBar and before the "Lessons" heading
       - Wrap in a div with mt-6 spacing

    4. Create `app/template.tsx`:
       - 'use client' directive
       - Import { motion } from 'motion/react'
       - Export default Template component
       - Wrap children in motion.div with:
         - initial={{ opacity: 0, y: 8 }}
         - animate={{ opacity: 1, y: 0 }}
         - transition={{ duration: 0.2, ease: 'easeOut' }}
       - Per research: do NOT use AnimatePresence here — template.tsx remounts on navigation, so initial/animate fire naturally. Skip exit animations for v1 (see Research open question 3).

    5. Add micro-animations (ANIM-02):

       a. In `components/lesson-complete-button.tsx`:
          - Import `{ motion } from 'motion/react'`
          - Replace each `<Button ...>` with `<motion.button>` wrapping (or use `motion.create(Button)` if it works with shadcn). The simplest approach: wrap each Button's `onClick` handler and content in a `<motion.div>` parent, OR replace `<Button>` with a `<motion.button>` that applies the same Tailwind classes. Preferred approach: wrap each `<Button>` in a `<motion.div whileHover={{ scale: 1.03 }} whileTap={{ scale: 0.97 }} transition={{ type: 'spring', stiffness: 400, damping: 17 }}>` so that the shadcn Button internals are untouched.
          - Both the "Mark Complete" and "Completed" buttons get the same motion wrapper.

       b. In `components/code-runner/code-runner-client.tsx`:
          - Import `{ motion } from 'motion/react'`
          - Wrap the Run `<Button size="sm" ...>` in a `<motion.div whileHover={{ scale: 1.03 }} whileTap={{ scale: 0.97 }} transition={{ type: 'spring', stiffness: 400, damping: 17 }} className="inline-flex">` so the Run button gets tactile hover/tap feedback.
          - Do NOT wrap the disabled state differently — motion handles disabled naturally (no hover scale when pointer-events are blocked by `disabled`).

       c. In `app/courses/[courseSlug]/page.tsx`: add `transition-transform hover:scale-[1.01]` to the existing lesson link className for CSS-based hover scale on lesson cards.

    6. Write tests:

       `__tests__/components/notebook-lm-card.test.tsx`:
       - Test renders explanation text containing "NotebookLM"
       - Test renders external link with href matching NOTEBOOK_URLS for '01-python-fundamentals'
       - Test renders "Google account" notice text
       - Test returns null for unknown courseSlug (render result is empty)

       `__tests__/components/template.test.tsx`:
       - Mock motion/react: motion.div renders as a regular div with data-testid
       - Test Template renders children
       - Test the wrapper div exists (the motion.div)

       `__tests__/components/micro-animations.test.tsx`:
       - Mock motion/react: mock `motion` so that `motion.div` renders a `<div>` and forwards all props including `whileHover`, `whileTap` as data attributes (e.g., `vi.mock('motion/react', () => { const motion = { div: React.forwardRef((props, ref) => <div data-whilehover={JSON.stringify(props.whileHover)} data-whiletap={JSON.stringify(props.whileTap)} {...filterMotionProps(props)} ref={ref} />) }; return { motion } })`
       - Mock the Zustand store for LessonCompleteButton
       - Mock usePyodideWorker, CodeMirror, and next-themes for CodeRunnerClient
       - Test: LessonCompleteButton renders with a parent element that has data-whilehover attribute (confirms motion.div wrapper with whileHover is present)
       - Test: CodeRunnerClient renders a Run button with a parent element that has data-whiletap attribute (confirms motion.div wrapper with whileTap is present)

    7. Verify: `pnpm test` all tests pass, `pnpm build` succeeds
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose __tests__/components/notebook-lm-card.test.tsx __tests__/components/template.test.tsx __tests__/components/micro-animations.test.tsx && pnpm build</automated>
  </verify>
  <done>
    - NotebookLMCard renders on all 12 course pages with explanation text and link (NBLM-01, NBLM-02)
    - Placeholder URLs in lib/notebook-urls.ts ready for user to replace (NBLM-03 setup)
    - Page transitions animate on route changes via template.tsx (ANIM-01)
    - LessonCompleteButton has motion whileHover/whileTap micro-animation (ANIM-02)
    - Code runner Run button has motion whileHover/whileTap micro-animation (ANIM-02)
    - Lesson cards have CSS hover micro-animation (ANIM-02)
    - All tests pass, build succeeds
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create 12 NotebookLM notebooks and update URLs</name>
  <action>
    This task requires manual creation of NotebookLM notebooks — there is no API or CLI for this.
  </action>
  <instructions>
    **NBLM-03: Create 12 pre-seeded NotebookLM notebooks**

    Requirements:
    - A personal Google account (Workspace accounts may not have public sharing enabled)
    - ~1 hour of time

    Steps for EACH of the 12 courses:

    1. Go to https://notebooklm.google.com
    2. Click "New Notebook"
    3. Name it after the course (e.g., "Python Fundamentals")
    4. Click "Add Source" and upload all lesson `.md` files from `courses/{courseSlug}/`
       - Course files are in: `courses/01-python-fundamentals/`, `courses/02-data-types-variables/`, etc.
       - Upload ALL lesson-*.md files for that course (do NOT upload README.md)
    5. After sources are added, click the Share button
    6. Enable "Public" sharing (creates a public URL)
    7. Copy the public share URL

    After creating all 12:

    8. Open `lib/notebook-urls.ts` in the project
    9. Replace each `PLACEHOLDER_{slug}` URL with the actual NotebookLM public share URL
    10. Save the file

    Course slugs (in order):
    - 01-python-fundamentals
    - 02-data-types-variables
    - 03-control-flow-logic
    - 04-functions-modules
    - 05-data-structures
    - 06-oop
    - 07-file-handling-exceptions
    - 08-working-with-libraries
    - 09-web-development-basics
    - 10-data-analysis-visualization
    - 11-automation-scripting
    - 12-capstone-best-practices
  </instructions>
  <resume-signal>Type "notebooks created" when all 12 URLs have been added to lib/notebook-urls.ts, or "skip notebooks" to proceed with placeholder URLs for now</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm test` — all existing + new tests pass
2. `pnpm build` — static pages generated without errors
3. `pnpm dev` — visit a course page, verify NotebookLM card is visible with explanation text
4. Click "Open in NotebookLM" — opens correct URL in new tab
5. Navigate between pages — observe smooth fade-in transition
6. Hover over lesson cards — observe subtle scale animation
7. Hover over LessonCompleteButton — observe motion scale-up animation
8. Click Run button in code runner — observe motion tap-down animation
</verification>

<success_criteria>
- NotebookLM card visible on every course page with explanation and link (NBLM-01, NBLM-02)
- 12 NotebookLM notebooks created with course materials (NBLM-03) — or placeholder URLs documented for later
- Page transitions animate on every route change (ANIM-01)
- LessonCompleteButton and Run button have motion whileHover/whileTap micro-animations (ANIM-02)
- Lesson cards have CSS hover micro-animation (ANIM-02)
- All tests green, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-differentiators-polish/03-03-SUMMARY.md`
</output>
