---
phase: 03-differentiators-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-search-index.ts
  - lib/search.ts
  - components/search/SearchDialog.tsx
  - components/search/SearchHighlight.tsx
  - components/site-header.tsx
  - __tests__/lib/search.test.ts
  - __tests__/components/search-dialog.test.tsx
  - package.json
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03]

must_haves:
  truths:
    - "Student can type a query in the global header search and see matching lessons instantly"
    - "Search results show highlighted matching text and link to lesson pages"
    - "Search is fuzzy — typos and partial matches still return relevant results"
    - "Search index is pre-built at build time, not computed on every page load"
  artifacts:
    - path: "scripts/generate-search-index.ts"
      provides: "Build-time script that reads all lessons and emits search-data.json + search-index.json to public/"
    - path: "lib/search.ts"
      provides: "highlight() utility for wrapping Fuse.js match ranges in <mark> elements"
      exports: ["highlight"]
    - path: "components/search/SearchDialog.tsx"
      provides: "Client component with search input, Fuse.js query, results list in a Dialog"
    - path: "components/search/SearchHighlight.tsx"
      provides: "Component that renders highlighted text from Fuse.js match indices"
    - path: "components/site-header.tsx"
      provides: "Updated header with search trigger button"
  key_links:
    - from: "scripts/generate-search-index.ts"
      to: "public/search-data.json"
      via: "fs.writeFileSync at build time"
      pattern: "writeFileSync.*search-data"
    - from: "scripts/generate-search-index.ts"
      to: "public/search-index.json"
      via: "fs.writeFileSync at build time"
      pattern: "writeFileSync.*search-index"
    - from: "components/search/SearchDialog.tsx"
      to: "public/search-data.json"
      via: "fetch() on dialog open"
      pattern: "fetch.*search-data"
    - from: "components/site-header.tsx"
      to: "components/search/SearchDialog.tsx"
      via: "import and render"
      pattern: "SearchDialog"
---

<objective>
Client-side fuzzy search across all lesson titles and course names using Fuse.js with a pre-built search index, accessible from a search button in the global site header.

Purpose: Let students quickly find any lesson across 120+ lessons by typing keywords, with instant fuzzy matching and highlighted results — no page reload, no server calls.

Output: Build-time search index generator, Fuse.js search dialog in global header, highlighted search results linking to lesson pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiators-polish/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts from the existing codebase -->

From lib/content.ts:
```typescript
export type LessonMeta = {
  slug: string
  courseSlug: string
  title: string
  lessonNumber: number
  duration: string
  level: string
  prev: string | null
  next: string | null
}

export type Course = {
  slug: string
  title: string
  description: string
  level: string
  lessonCount: number
  lessons: LessonMeta[]
}

export function getAllCourses(): Course[]
```

From components/site-header.tsx (will be modified):
```typescript
import Link from 'next/link'
import { ThemeToggle } from '@/components/theme-toggle'

export function SiteHeader() {
  return (
    <header className="sticky top-0 z-40 h-16 border-b bg-background">
      <div className="max-w-screen-2xl mx-auto px-4 h-full flex items-center justify-between">
        <Link href="/courses" className="font-semibold text-lg hover:text-muted-foreground transition-colors">
          Python Lessons
        </Link>
        <ThemeToggle />
      </div>
    </header>
  )
}
```

Existing shadcn/ui components available: Dialog, Button, Badge, Breadcrumb, Sheet
Lucide icons available (already in package.json): Search icon
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create search index generator and highlight utility</name>
  <files>
    scripts/generate-search-index.ts,
    lib/search.ts,
    components/search/SearchHighlight.tsx,
    __tests__/lib/search.test.ts,
    package.json
  </files>
  <behavior>
    - Test: highlight("Hello World", [[0,4]]) wraps "Hello" in a mark element
    - Test: highlight("test", []) returns plain text with no marks
    - Test: highlight with multiple ranges wraps each range independently
    - Test: SearchHighlight component renders mark elements for matched indices
    - Test: generate script produces valid JSON with expected fields (id, title, courseTitle, href)
  </behavior>
  <action>
    1. Ensure `tsx` is in devDependencies (needed for running TS scripts): `pnpm add -D tsx` if not already present

    2. Create `scripts/generate-search-index.ts`:
       - Import Fuse from 'fuse.js', fs, path
       - Import getAllCourses from '../lib/content' (relative path since this runs outside Next.js)
       - Build a flat lessons array: for each course, for each lesson, create:
         `{ id: "{courseSlug}/{slug}", title: lesson.title, courseTitle: course.title, href: "/courses/{courseSlug}/{slug}" }`
       - Define search keys: ['title', 'courseTitle']
       - Create index: `Fuse.createIndex(keys, lessons)`
       - Write `public/search-data.json` (the lessons array)
       - Write `public/search-index.json` (index.toJSON())
       - Log count: "Generated search index for N lessons"
       - Ensure `public/` directory exists before writing

    3. Add to package.json scripts:
       - `"generate:search": "tsx scripts/generate-search-index.ts"`
       - Update `"prebuild"` to: `"prebuild": "tsx scripts/generate-search-index.ts"`
         NOTE: Only add the search generator to prebuild. Plan 03-01 independently adds `tsx scripts/generate-mindmap-data.ts` to prebuild when it runs. Since 03-01 and 03-02 are Wave 1 (parallel), do NOT reference generate-mindmap-data.ts here — that script does not exist until 03-01 creates it. Whichever plan runs second will see the other's prebuild entry and append via `&&`. If 03-01 runs first, it sets prebuild to the mindmap script; this plan then appends the search script. If this plan runs first, it sets prebuild to the search script; 03-01 then appends the mindmap script.
       - Add `"predev"` with same content as prebuild (for local dev)

    4. Run the generator script to produce the initial JSON files

    5. Add `public/search-data.json` and `public/search-index.json` to .gitignore (generated at build time)

    6. Create `lib/search.ts`:
       - Export `highlight` function that takes (value: string, indices: readonly [number, number][]) and returns React.ReactNode
       - Use the recursive Fuse.js highlight pattern from research: split string at match boundaries, wrap matched substrings in `<mark className="bg-yellow-200 dark:bg-yellow-800">` elements
       - Keep it simple: iterate indices array, build fragments array with alternating plain text and mark-wrapped text

    7. Create `components/search/SearchHighlight.tsx`:
       - Import highlight from lib/search
       - Export `SearchHighlight` component: takes { value: string, indices: readonly [number, number][] }
       - Renders the result of highlight(value, indices)

    8. Write tests in `__tests__/lib/search.test.ts`:
       - Test highlight function output structure
       - Test with no indices returns plain string
       - Test with single range wraps correctly
       - Test with multiple ranges wraps each independently
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose __tests__/lib/search.test.ts</automated>
  </verify>
  <done>
    - generate-search-index.ts produces search-data.json and search-index.json in public/
    - highlight() correctly wraps match ranges in mark elements
    - SearchHighlight component renders highlighted text
    - prebuild/predev scripts configured in package.json (search generator only; mindmap generator added by 03-01)
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SearchDialog component and wire into SiteHeader</name>
  <files>
    components/search/SearchDialog.tsx,
    components/site-header.tsx,
    __tests__/components/search-dialog.test.tsx
  </files>
  <behavior>
    - Test: SearchDialog renders a search input field
    - Test: SearchDialog shows results when query is typed (mock Fuse.js)
    - Test: Each result renders as a link to the lesson page
    - Test: Results show highlighted title text
    - Test: No results shown for empty query
    - Test: SiteHeader renders a search trigger button
  </behavior>
  <action>
    1. Create `components/search/SearchDialog.tsx`:
       - 'use client' directive
       - Import { useState, useEffect, useDeferredValue, useCallback } from 'react'
       - Import Fuse from 'fuse.js'
       - Import Link from 'next/link'
       - Import { Dialog, DialogContent, DialogTrigger } from '@/components/ui/dialog'
       - Import { Button } from '@/components/ui/button'
       - Import { Search } from 'lucide-react'
       - Import { SearchHighlight } from './SearchHighlight'
       - DO NOT import search-data.json at top level (file may not exist at import time). Instead, lazy-load via fetch('/search-data.json') and fetch('/search-index.json') when dialog opens.
       - State: open (boolean), query (string), results (FuseResult[]), fuseInstance (Fuse | null), loading (boolean)
       - On dialog open: if fuseInstance is null, fetch both JSON files, create Fuse instance with parseIndex, store in state
       - Use useDeferredValue for query to avoid blocking input on search
       - When deferredQuery.length >= 2, call fuse.search(deferredQuery, { limit: 10 })
       - Render results as a scrollable list: each result shows course title (muted), lesson title with highlighted matches, and links to lesson href
       - Keyboard shortcut: Cmd+K (Mac) / Ctrl+K (Windows) opens the dialog. Use useEffect with keydown listener.
       - Close dialog on result click (set open to false)
       - Empty state: "Type at least 2 characters to search" when query is short
       - No results state: "No lessons found for '{query}'" when search returns empty

    2. Modify `components/site-header.tsx`:
       - This file is currently a Server Component (no 'use client'). The SearchDialog is a Client Component.
       - Import SearchDialog from '@/components/search/SearchDialog'
       - Add SearchDialog between the logo Link and ThemeToggle in the header flex container
       - Wrap the search trigger area: `<div className="flex items-center gap-2"><SearchDialog /><ThemeToggle /></div>`
       - SiteHeader remains a Server Component — SearchDialog is imported as a Client Component child (this is valid in Next.js)

    3. Write tests in `__tests__/components/search-dialog.test.tsx`:
       - Mock fuse.js to return predictable results
       - Mock fetch to return test search-data.json and search-index.json
       - Test: renders search button/trigger
       - Test: dialog opens on trigger click
       - Test: input field is present in opened dialog
       - Test: results appear for a valid query (via mocked Fuse)
       - Test: each result is a link with correct href
       - Test: "No lessons found" message for empty results

    4. Verify: `pnpm test` all tests pass, `pnpm build` succeeds
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose __tests__/components/search-dialog.test.tsx && pnpm build</automated>
  </verify>
  <done>
    - SearchDialog opens from header with Cmd+K shortcut or click (SRCH-03)
    - Typing a query shows fuzzy-matched lessons with highlighted titles (SRCH-01, SRCH-02)
    - Results link to lesson pages, clicking closes dialog
    - Search index loaded lazily on first dialog open (not at page load)
    - SiteHeader displays search trigger button alongside theme toggle
    - All tests pass, build succeeds
  </done>
</task>

</tasks>

<verification>
1. `pnpm test` — all existing + new tests pass
2. `pnpm build` — static pages generated (prebuild generates search index)
3. `pnpm dev` — click search icon in header, type "loop", verify matching lessons appear with highlighted text
4. Click a search result — navigates to lesson page, dialog closes
5. Cmd+K keyboard shortcut opens search dialog
6. Search for partial/misspelled term — fuzzy matching returns relevant results
</verification>

<success_criteria>
- Student can access search from global header on any page (SRCH-03)
- Fuzzy search returns relevant lessons for partial matches (SRCH-01)
- Results show highlighted match text and link to lesson pages (SRCH-02)
- Search index built at build time, loaded lazily on first use
- All tests green, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-differentiators-polish/03-02-SUMMARY.md`
</output>
