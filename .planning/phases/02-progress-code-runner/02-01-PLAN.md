---
phase: 02-progress-code-runner
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/store/progress.ts
  - components/progress-hydration.tsx
  - components/course-sidebar.tsx
  - components/mobile-sidebar-trigger.tsx
  - components/lesson-complete-button.tsx
  - components/course-progress-bar.tsx
  - app/layout.tsx
  - app/courses/page.tsx
  - app/courses/[courseSlug]/page.tsx
  - app/courses/[courseSlug]/[lessonSlug]/page.tsx
  - __tests__/lib/progress-store.test.ts
  - __tests__/components/course-sidebar.test.tsx
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03

must_haves:
  truths:
    - "Student can mark a lesson complete and the state survives browser refresh"
    - "Course catalog cards show completion percentage for each course"
    - "Course overview page shows completion percentage"
    - "Sidebar lesson list shows distinct icons for completed vs not-started lessons"
  artifacts:
    - path: "lib/store/progress.ts"
      provides: "Zustand progress store with persist middleware"
      exports: ["useProgressStore"]
      contains: "skipHydration: true"
    - path: "components/progress-hydration.tsx"
      provides: "Root-level rehydration component"
      contains: "rehydrate()"
    - path: "components/lesson-complete-button.tsx"
      provides: "Mark complete toggle button for lesson pages"
      contains: "markComplete"
    - path: "components/course-progress-bar.tsx"
      provides: "Progress percentage display for catalog and course pages"
      contains: "getCourseProgress"
    - path: "__tests__/lib/progress-store.test.ts"
      provides: "Unit tests for progress store"
      min_lines: 40
  key_links:
    - from: "components/lesson-complete-button.tsx"
      to: "lib/store/progress.ts"
      via: "useProgressStore hook"
      pattern: "useProgressStore"
    - from: "components/course-sidebar.tsx"
      to: "lib/store/progress.ts"
      via: "isComplete selector"
      pattern: "useProgressStore.*isComplete"
    - from: "components/progress-hydration.tsx"
      to: "lib/store/progress.ts"
      via: "persist.rehydrate() in useEffect"
      pattern: "useProgressStore\\.persist\\.rehydrate"
    - from: "app/layout.tsx"
      to: "components/progress-hydration.tsx"
      via: "ProgressHydration rendered in root layout"
      pattern: "<ProgressHydration"
---

<objective>
Zustand progress store with localStorage persistence, lesson completion toggle, course progress percentages on catalog/overview pages, and sidebar completion indicators.

Purpose: Students need persistent progress tracking so they can see which lessons they've completed, track course progress percentages, and visually distinguish completed from unstarted lessons in the sidebar.

Output: Working progress system with Zustand store, UI components, and unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-progress-code-runner/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/content.ts:
```typescript
export type LessonMeta = {
  slug: string
  courseSlug: string
  title: string
  lessonNumber: number
  duration: string
  level: string
  prev: string | null
  next: string | null
}

export type Course = {
  slug: string
  title: string
  description: string
  level: string
  lessonCount: number
  lessons: LessonMeta[]
}

export function getAllCourses(): Course[]
export function getCourse(courseSlug: string): Course | undefined
export function getLesson(courseSlug: string, lessonSlug: string): LessonMeta | undefined
```

From components/course-sidebar.tsx:
```typescript
// 'use client' — already a Client Component using usePathname
type CourseSidebarProps = {
  courseSlug: string
  lessons: LessonMeta[]
}
export function CourseSidebar({ courseSlug, lessons }: CourseSidebarProps)
```

From app/layout.tsx:
```typescript
// Root layout — SiteHeader + ThemeProvider wrap children
// ProgressHydration must be added inside ThemeProvider, before children
```

From app/courses/page.tsx:
```typescript
// Server Component — calls getAllCourses() and renders course cards
// CourseProgressBar must be a Client Component child within each card
```

From app/courses/[courseSlug]/page.tsx:
```typescript
// Server Component — calls getCourse(courseSlug) and renders lesson list
// Progress percentage must be a Client Component child
```

From app/courses/[courseSlug]/[lessonSlug]/page.tsx:
```typescript
// Server Component — renders LessonContent MDX, LessonBreadcrumb, LessonNav
// LessonCompleteButton must be added below article or above LessonNav
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zustand progress store with localStorage persistence and test suite</name>
  <files>lib/store/progress.ts, components/progress-hydration.tsx, __tests__/lib/progress-store.test.ts</files>
  <behavior>
    - markComplete('course-01', 'lesson-01') adds lessonSlug to completedLessons['course-01']
    - markComplete same lesson twice does not duplicate the entry
    - markIncomplete('course-01', 'lesson-01') removes lessonSlug from completedLessons['course-01']
    - markIncomplete non-existent lesson is a no-op (no error)
    - isComplete('course-01', 'lesson-01') returns true after markComplete, false after markIncomplete
    - isComplete returns false for never-marked lesson
    - getCourseProgress('course-01', 10) returns 10 when 1 of 10 lessons completed
    - getCourseProgress returns 0 for course with no completions
    - getCourseProgress returns 0 when totalLessons is 0 (division safety)
    - Store persists to localStorage under key 'python-course-progress'
    - Store uses skipHydration: true to prevent SSR hydration mismatch
  </behavior>
  <action>
    **TDD RED phase:** Create `__tests__/lib/progress-store.test.ts` with all tests listed in behavior. Mock localStorage using `vitest.stubGlobal`. Import `useProgressStore` from `@/lib/store/progress`. Use `act(() => useProgressStore.getState().markComplete(...))` pattern to test store actions. Run `pnpm test -- __tests__/lib/progress-store.test.ts` — tests MUST fail (RED).

    **TDD GREEN phase:** Create `lib/store/progress.ts` following the exact pattern from 02-RESEARCH.md Pattern 1:
    - `create<ProgressState>()(persist(...))`
    - State shape: `completedLessons: Record<string, string[]>`, `markComplete`, `markIncomplete`, `isComplete`, `getCourseProgress`
    - Persist config: `name: 'python-course-progress'`, `storage: createJSONStorage(() => localStorage)`, `skipHydration: true`
    - Use `zustand/shallow` `useShallow` in a comment noting consumers should use it for array selectors (per Pitfall 6 from research)

    Create `components/progress-hydration.tsx`:
    - `'use client'` directive
    - Calls `useProgressStore.persist.rehydrate()` inside `useEffect([], [])` on mount
    - Returns `null` (renders nothing)

    Run `pnpm test -- __tests__/lib/progress-store.test.ts` — all tests MUST pass (GREEN).

    **Install dependency:** `pnpm add zustand` before creating files.
  </action>
  <verify>
    <automated>pnpm test -- __tests__/lib/progress-store.test.ts</automated>
  </verify>
  <done>
    - All progress store tests pass (markComplete, markIncomplete, isComplete, getCourseProgress, idempotency, division safety)
    - Store uses persist middleware with skipHydration: true
    - ProgressHydration component exists and calls rehydrate() on mount
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire progress UI into sidebar, catalog, course page, and lesson page</name>
  <files>components/lesson-complete-button.tsx, components/course-progress-bar.tsx, components/course-sidebar.tsx, components/mobile-sidebar-trigger.tsx, app/layout.tsx, app/courses/page.tsx, app/courses/[courseSlug]/page.tsx, app/courses/[courseSlug]/[lessonSlug]/page.tsx, __tests__/components/course-sidebar.test.tsx</files>
  <action>
    **Create `components/lesson-complete-button.tsx`:**
    - `'use client'` directive
    - Props: `{ courseSlug: string; lessonSlug: string }`
    - Reads `isComplete(courseSlug, lessonSlug)` from `useProgressStore`
    - Renders a shadcn Button: if complete shows "Completed" (green check icon, outline variant), if not shows "Mark Complete" (circle icon, default variant)
    - On click toggles: calls `markComplete` or `markIncomplete`
    - Use `CheckCircle2` and `Circle` from lucide-react

    **Create `components/course-progress-bar.tsx`:**
    - `'use client'` directive
    - Props: `{ courseSlug: string; totalLessons: number }`
    - Reads `getCourseProgress(courseSlug, totalLessons)` from `useProgressStore`
    - Renders a small progress bar (div with bg-primary width set to percentage) and text like "3/10 completed"
    - Count completed lessons using `useProgressStore(s => (s.completedLessons[courseSlug] ?? []).length)` — use `useShallow` from `zustand/shallow` for the array selector to prevent re-render loops (Pitfall 6)
    - If 0% completed, render nothing (clean look for new students)

    **Modify `components/course-sidebar.tsx`:**
    - Import `useProgressStore` from `@/lib/store/progress`
    - Read `isComplete` from store
    - For each lesson item, render a small icon before the lesson number:
      - Completed: `CheckCircle2` icon (lucide, size 14, text-green-500 dark:text-green-400)
      - Not started: `Circle` icon (lucide, size 14, text-muted-foreground)
    - Keep all existing functionality (active highlight, links, lesson numbers)

    **Modify `components/mobile-sidebar-trigger.tsx`:**
    - Same completion indicator as course-sidebar (CheckCircle2/Circle icons per lesson)

    **Modify `app/layout.tsx`:**
    - Import `ProgressHydration` from `@/components/progress-hydration`
    - Render `<ProgressHydration />` inside the `<ThemeProvider>` block, before `{children}`

    **Modify `app/courses/page.tsx`:**
    - Import `CourseProgressBar` from `@/components/course-progress-bar`
    - Inside each course Card (after the CardDescription), render `<CourseProgressBar courseSlug={course.slug} totalLessons={course.lessonCount} />`
    - CourseProgressBar is a Client Component — it can be rendered inside a Server Component as a leaf

    **Modify `app/courses/[courseSlug]/page.tsx`:**
    - Import `CourseProgressBar` from `@/components/course-progress-bar`
    - Render `<CourseProgressBar courseSlug={courseSlug} totalLessons={course.lessonCount} />` after the course description paragraph and before the "Lessons" heading

    **Modify `app/courses/[courseSlug]/[lessonSlug]/page.tsx`:**
    - Import `LessonCompleteButton` from `@/components/lesson-complete-button`
    - Render `<LessonCompleteButton courseSlug={courseSlug} lessonSlug={lessonSlug} />` between the article and the LessonNav component

    **Extend `__tests__/components/course-sidebar.test.tsx`:**
    - Add mock for `@/lib/store/progress`: `vi.mock('@/lib/store/progress', () => ({ useProgressStore: vi.fn() }))`
    - Mock `useProgressStore` to return `{ isComplete: vi.fn().mockReturnValue(false) }` by default
    - Add test: "renders CheckCircle2 icon for completed lesson" — mock `isComplete` to return true for lesson-01, assert the completed icon is present
    - Add test: "renders Circle icon for not-started lesson" — mock `isComplete` to return false, assert the not-started icon is present

    Run `pnpm test` to verify all existing tests still pass plus new sidebar completion tests.
    Run `pnpm build` to verify static build succeeds (no SSR hydration issues from the new Client Components).
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <done>
    - LessonCompleteButton toggles completion state on lesson pages
    - CourseProgressBar shows percentage on catalog and course pages (hidden when 0%)
    - Sidebar shows CheckCircle2 for completed lessons and Circle for not-started
    - ProgressHydration renders in root layout
    - All existing + new tests pass
    - pnpm build succeeds with no hydration errors
  </done>
</task>

</tasks>

<verification>
- `pnpm test` — all unit tests pass (progress store + sidebar completion indicators)
- `pnpm build` — static build succeeds with new Client Components
- Manual: open lesson page, click "Mark Complete", refresh — state persists
- Manual: check catalog page shows progress bar after completing a lesson
- Manual: sidebar shows green check on completed lessons
</verification>

<success_criteria>
1. markComplete/markIncomplete correctly modifies localStorage-persisted state
2. getCourseProgress returns accurate percentage
3. Catalog and course pages display completion percentages
4. Sidebar shows completed/not-started icons per lesson
5. Lesson page has toggle button for marking completion
6. All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-progress-code-runner/02-01-SUMMARY.md`
</output>
