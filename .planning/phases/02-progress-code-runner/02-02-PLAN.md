---
phase: 02-progress-code-runner
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - public/workers/pyodide.worker.mjs
  - hooks/use-pyodide-worker.ts
  - components/code-runner/index.tsx
  - components/code-runner/code-runner-client.tsx
  - components/code-runner/output-panel.tsx
  - app/courses/[courseSlug]/[lessonSlug]/page.tsx
  - __tests__/hooks/use-pyodide-worker.test.ts
  - __tests__/components/code-runner.test.tsx
autonomous: false
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
  - CODE-05
  - CODE-06

must_haves:
  truths:
    - "Student can write Python code in an in-lesson editor with syntax highlighting"
    - "Student can click Run and see stdout/stderr output in an adjacent panel"
    - "Pyodide loads only when Run is first clicked, not on page load"
    - "A clear loading indicator shows during Pyodide cold start"
    - "Lesson content stays fully readable while code runner loads"
  artifacts:
    - path: "public/workers/pyodide.worker.mjs"
      provides: "Self-contained Pyodide Web Worker"
      contains: "loadPyodide"
    - path: "hooks/use-pyodide-worker.ts"
      provides: "React hook for lazy worker instantiation and run execution"
      exports: ["usePyodideWorker"]
      contains: "workerRef"
    - path: "components/code-runner/code-runner-client.tsx"
      provides: "CodeMirror editor + run button + output panel"
      contains: "CodeMirror"
    - path: "components/code-runner/index.tsx"
      provides: "Dynamic import wrapper with ssr: false"
      contains: "ssr: false"
    - path: "components/code-runner/output-panel.tsx"
      provides: "stdout/stderr display component"
      contains: "stderr"
    - path: "__tests__/hooks/use-pyodide-worker.test.ts"
      provides: "Tests for lazy worker instantiation"
      min_lines: 20
    - path: "__tests__/components/code-runner.test.tsx"
      provides: "Tests for editor rendering, output display, loading states"
      min_lines: 30
  key_links:
    - from: "components/code-runner/code-runner-client.tsx"
      to: "hooks/use-pyodide-worker.ts"
      via: "usePyodideWorker hook"
      pattern: "usePyodideWorker"
    - from: "hooks/use-pyodide-worker.ts"
      to: "public/workers/pyodide.worker.mjs"
      via: "new Worker('/workers/pyodide.worker.mjs', { type: 'module' })"
      pattern: "new Worker.*pyodide\\.worker"
    - from: "components/code-runner/index.tsx"
      to: "components/code-runner/code-runner-client.tsx"
      via: "next/dynamic with ssr: false"
      pattern: "dynamic.*ssr.*false"
    - from: "app/courses/[courseSlug]/[lessonSlug]/page.tsx"
      to: "components/code-runner/index.tsx"
      via: "CodeRunner component rendered on lesson page"
      pattern: "<CodeRunner"
---

<objective>
In-browser Python code runner with Pyodide Web Worker, CodeMirror 6 editor, lazy loading on first Run click, and stdout/stderr output panel.

Purpose: Students need to practice Python directly in the lesson page without any local setup. The code runner must load lazily (Pyodide is ~30MB WASM) and execute in a Web Worker to keep the UI responsive.

Output: Working code runner embedded in every lesson page, with unit tests for testable components and manual verification for Pyodide execution.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-progress-code-runner/02-RESEARCH.md
@.planning/phases/02-progress-code-runner/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From hooks/use-pyodide-worker.ts (to be created):
```typescript
type RunStatus = 'idle' | 'loading' | 'ready' | 'running'
type RunResult = {
  output: { type: 'stdout' | 'stderr'; line: string }[]
  error: string | null
}
export function usePyodideWorker(): {
  run: (code: string) => Promise<RunResult>
  status: RunStatus
}
```

From components/code-runner/index.tsx (to be created):
```typescript
// Dynamic import wrapper — renders CodeRunnerClient with ssr: false
export function CodeRunner({ initialCode }: { initialCode: string })
```

From app/courses/[courseSlug]/[lessonSlug]/page.tsx (existing):
```typescript
// Server Component — after plan 02-01 it has LessonCompleteButton
// CodeRunner will be added below the article, before LessonCompleteButton
```

From next-themes (already installed):
```typescript
// useTheme() provides resolvedTheme for CodeMirror theme switching
import { useTheme } from 'next-themes'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pyodide Web Worker, usePyodideWorker hook, CodeRunner components, and test suite</name>
  <files>public/workers/pyodide.worker.mjs, hooks/use-pyodide-worker.ts, components/code-runner/index.tsx, components/code-runner/code-runner-client.tsx, components/code-runner/output-panel.tsx, __tests__/hooks/use-pyodide-worker.test.ts, __tests__/components/code-runner.test.tsx</files>
  <behavior>
    - usePyodideWorker: Worker constructor is NOT called on hook mount (lazy instantiation)
    - usePyodideWorker: Worker constructor IS called on first run() invocation
    - usePyodideWorker: status starts as 'idle'
    - usePyodideWorker: status transitions to 'loading' when worker is first created
    - CodeRunner: renders a CodeMirror editor area (test for a container div, since CodeMirror needs real DOM)
    - CodeRunner: renders a Run button
    - CodeRunner: renders an output panel area
    - OutputPanel: displays stdout lines
    - OutputPanel: displays stderr lines with distinct styling (text-red-500)
    - CodeRunner: shows loading spinner/text when status is 'loading'
    - CodeRunner: Run button is disabled when status is 'running'
  </behavior>
  <action>
    **Install dependencies:** `pnpm add @uiw/react-codemirror @codemirror/lang-python @uiw/codemirror-theme-github`

    **TDD RED phase:** Create test files first.

    `__tests__/hooks/use-pyodide-worker.test.ts`:
    - Mock the global `Worker` constructor: `vi.stubGlobal('Worker', vi.fn(() => ({ postMessage: vi.fn(), onmessage: null, terminate: vi.fn() })))`
    - Test: "does not create Worker on mount" — render hook via `renderHook(() => usePyodideWorker())`, assert `Worker` constructor was NOT called
    - Test: "creates Worker on first run() call" — call `result.current.run('print("hi")')`, assert `Worker` constructor was called once
    - Test: "status is idle initially" — assert `result.current.status === 'idle'`

    `__tests__/components/code-runner.test.tsx`:
    - Mock `@uiw/react-codemirror` to render a `<textarea data-testid="mock-editor" />`
    - Mock `next-themes` `useTheme` to return `{ resolvedTheme: 'light' }`
    - Mock `@/hooks/use-pyodide-worker` to return `{ run: vi.fn(), status: 'idle' }`
    - Test: "renders editor" — assert `data-testid="mock-editor"` exists
    - Test: "renders Run button" — assert button with text "Run" exists
    - Test: "renders output panel" — assert output panel container exists
    - Test: "output panel displays stdout" — render OutputPanel with stdout data, assert lines visible
    - Test: "output panel displays stderr with red text" — render OutputPanel with stderr data, assert red styling class
    - Test: "shows loading indicator when status is loading" — mock status as 'loading', assert loading text/spinner visible
    - Test: "Run button disabled when running" — mock status as 'running', assert button is disabled

    Run tests — MUST fail (RED).

    **TDD GREEN phase:**

    **Create `public/workers/pyodide.worker.mjs`:**
    - ESM module worker (no bundler)
    - Import `loadPyodide` from `https://cdn.jsdelivr.net/pyodide/v0.29.3/full/pyodide.mjs`
    - Module-level `let pyodideReady = null` — cached promise (CODE-03: lazy, CODE-04: no repeated downloads)
    - `self.onmessage` handler: receives `{ id, code }`, lazily initializes `pyodideReady = loadPyodide()`, awaits it, configures `setStdout({ batched })` and `setStderr({ batched })` (CODE-05), runs `runPythonAsync(code)`, sends back `{ id, output, error }` via `postMessage`
    - On error: sends `{ id, output, error: err.message }`

    **Create `hooks/use-pyodide-worker.ts`:**
    - `'use client'` directive
    - Module-level singleton: `let sharedWorker: Worker | null = null` and `let sharedWorkerReady: Promise<void> | null = null` — shared across all hook instances (Open Question 3 from research: multiple code runners on same page)
    - `usePyodideWorker()` hook returns `{ run, status }`
    - `status` state: `'idle' | 'loading' | 'ready' | 'running'`
    - `run(code)` function: if `sharedWorker` is null, creates `new Worker('/workers/pyodide.worker.mjs', { type: 'module' })`, sets status to `'loading'`; generates unique `id` (incrementing counter), sets status to `'running'`, posts `{ id, code }`, returns Promise resolved by `onmessage` when matching `id` received, sets status to `'ready'`
    - Cleanup: `useEffect` cleanup terminates worker on unmount only if this is the last hook instance (use a ref counter)

    **Create `components/code-runner/output-panel.tsx`:**
    - Props: `{ output: { type: 'stdout' | 'stderr'; line: string }[]; error: string | null }`
    - Renders each output line in a `<pre>` block with monospace font
    - stdout lines: default text color
    - stderr lines: `text-red-500 dark:text-red-400`
    - If `error` is non-null, render error message at the bottom with red styling
    - If output is empty and error is null, render a muted placeholder: "Output will appear here..."
    - Export `OutputPanel` as a named export

    **Create `components/code-runner/code-runner-client.tsx`:**
    - `'use client'` directive
    - Import `CodeMirror` from `@uiw/react-codemirror`, `python` from `@codemirror/lang-python`, `githubLight`/`githubDark` from `@uiw/codemirror-theme-github`
    - Import `useTheme` from `next-themes` for dark/light mode matching
    - Import `usePyodideWorker` from `@/hooks/use-pyodide-worker`
    - Import `OutputPanel` from `./output-panel`
    - State: `code` (initialized from `initialCode` prop), `output` (array of stdout/stderr lines), `error` (string | null)
    - CodeMirror editor: `value={code}`, `onChange={setCode}`, `extensions={[python()]}`, `theme={resolvedTheme === 'dark' ? githubDark : githubLight}`
    - Run button: shadcn `Button` with `Play` icon from lucide-react; disabled when `status === 'running'`; onClick calls `run(code)` and sets output/error from result
    - Loading state (CODE-06): when `status === 'loading'`, show an overlay/inline text "Loading Python runtime..." with a `Loader2` spinner icon (lucide animate-spin)
    - Layout: editor on top (with min-height ~200px), Run button bar in middle, OutputPanel below

    **Create `components/code-runner/index.tsx`:**
    - Import `dynamic` from `next/dynamic`
    - Dynamically import `./code-runner-client` with `{ ssr: false }` (CODE-04 pitfall: CodeMirror accesses window)
    - Loading fallback: `<div className="h-64 animate-pulse bg-muted rounded-lg" />` (skeleton)
    - Export `CodeRunner` component with prop `{ initialCode: string }`

    Run tests — MUST pass (GREEN).
  </action>
  <verify>
    <automated>pnpm test -- __tests__/hooks/use-pyodide-worker.test.ts __tests__/components/code-runner.test.tsx</automated>
  </verify>
  <done>
    - Worker file exists at public/workers/pyodide.worker.mjs with CDN import and lazy loadPyodide
    - usePyodideWorker hook lazy-instantiates worker on first run(), not on mount
    - CodeMirror editor renders with Python syntax via next/dynamic ssr:false
    - OutputPanel displays stdout/stderr with distinct styling
    - Loading indicator shows during Pyodide cold start
    - Run button disabled during execution
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CodeRunner into lesson pages and verify build</name>
  <files>app/courses/[courseSlug]/[lessonSlug]/page.tsx</files>
  <action>
    **Modify `app/courses/[courseSlug]/[lessonSlug]/page.tsx`:**
    - Import `CodeRunner` from `@/components/code-runner`
    - Add the CodeRunner component between the article and the LessonCompleteButton (added by plan 02-01)
    - Wrap in a section with heading "Try it yourself" and a brief instruction text
    - Pass `initialCode` prop with a default Python snippet: `'# Write your Python code here\nprint("Hello, Python!")\n'`
    - Add margin spacing to separate from article and completion button

    ```tsx
    <section className="mt-10 mb-8">
      <h2 className="text-xl font-semibold mb-3">Try it yourself</h2>
      <p className="text-sm text-muted-foreground mb-4">
        Write Python code below and click Run to execute it in your browser.
      </p>
      <CodeRunner initialCode={'# Write your Python code here\nprint("Hello, Python!")\n'} />
    </section>
    ```

    Run `pnpm test` to verify all tests still pass.
    Run `pnpm build` to verify static build succeeds with the dynamically imported CodeRunner (ssr: false means it won't break SSG).
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <done>
    - CodeRunner component renders on every lesson page
    - pnpm build succeeds (no SSR/window errors from CodeMirror)
    - All existing tests still pass
    - CodeRunner appears between article content and lesson completion button
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Pyodide code runner works end-to-end in browser</name>
  <files>N/A</files>
  <action>
    Human verification of Pyodide Web Worker execution, lazy loading behavior, and UI responsiveness. This cannot be automated because Pyodide WASM and Web Worker APIs are not available in jsdom/vitest.
  </action>
  <verify>
    <automated>MANUAL — Pyodide WASM + Web Worker not available in jsdom</automated>
  </verify>
  <done>User confirms: Python executes in browser, lazy loads on first Run, UI stays responsive, stdout/stderr display correctly</done>
  <what-built>
    Complete code runner: Pyodide Web Worker loads Python WASM from CDN on first Run click, CodeMirror 6 editor with Python syntax highlighting and dark/light theme, stdout/stderr output panel, loading indicator during cold start.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and navigate to any lesson page (e.g., http://localhost:3000/courses/01-python-fundamentals/lesson-01)
    2. Scroll down to the "Try it yourself" section — verify CodeMirror editor renders with Python syntax highlighting
    3. Verify the editor matches current theme (switch dark/light mode to confirm)
    4. Type `print("Hello from Pyodide!")` in the editor
    5. Click "Run" — verify loading indicator appears briefly (2-3s on first run)
    6. Verify "Hello from Pyodide!" appears in the output panel below
    7. Type `print(1/0)` and click Run — verify stderr/error output appears in red
    8. While a long-running script executes (e.g., `for i in range(10000000): pass`), verify the lesson page remains scrollable and responsive (Web Worker not blocking UI)
    9. Verify the second Run click is faster (Pyodide already loaded, no cold start)
    10. Open browser DevTools Network tab — verify Pyodide WASM was NOT loaded on page load (only after first Run click)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test` — all unit tests pass (progress store + sidebar + code runner + hook)
- `pnpm build` — static build succeeds with all new Client Components
- Manual: CodeRunner on lesson page loads Pyodide on first Run click, displays output
- Manual: Loading indicator visible during cold start
- Manual: stderr shown in red, stdout in default color
- Manual: UI remains responsive during Python execution (Web Worker)
</verification>

<success_criteria>
1. Python code executes in browser via Pyodide WASM in Web Worker (CODE-01, CODE-02)
2. Pyodide loads lazily on first Run click, not on page load (CODE-03)
3. CodeMirror 6 editor with Python syntax highlighting matches site theme (CODE-04)
4. stdout/stderr displayed in output panel with distinct styling (CODE-05)
5. Loading indicator shown during Pyodide cold start (CODE-06)
6. All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-progress-code-runner/02-02-SUMMARY.md`
</output>
