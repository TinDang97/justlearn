---
phase: 13-webllm-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.mjs
  - components/webgpu-fallback.tsx
  - __tests__/lib/next-config-headers.test.ts
  - __tests__/components/webgpu-fallback.test.tsx
autonomous: true
requirements: [INFRA-01, INFRA-03]

must_haves:
  truths:
    - "next.config.mjs headers() returns COEP credentialless and COOP same-origin for all routes"
    - "WebGPUFallback component renders NotebookLM link when WebGPU is unavailable"
    - "COEP/COOP rule is first in the headers() return array so it is not shadowed by path-specific rules"
  artifacts:
    - path: "__tests__/lib/next-config-headers.test.ts"
      provides: "Header config validation tests"
      contains: "Cross-Origin-Embedder-Policy"
    - path: "__tests__/components/webgpu-fallback.test.tsx"
      provides: "Fallback component rendering tests"
      contains: "NotebookLM"
    - path: "components/webgpu-fallback.tsx"
      provides: "Graceful fallback UI for browsers without WebGPU"
      exports: ["WebGPUFallback"]
  key_links:
    - from: "next.config.mjs"
      to: "SharedArrayBuffer"
      via: "COEP/COOP headers enabling cross-origin isolation"
      pattern: "Cross-Origin-Embedder-Policy.*credentialless"
    - from: "components/webgpu-fallback.tsx"
      to: "NotebookLM"
      via: "fallback link prop"
      pattern: "notebookLmUrl"
---

<objective>
Add COEP/COOP headers to next.config.mjs for SharedArrayBuffer support and create the WebGPU fallback component that links to NotebookLM when WebGPU is unavailable.

Purpose: These two infrastructure pieces must exist before any WebLLM engine code. COEP/COOP headers enable SharedArrayBuffer (required by WebGPU). The fallback component gates all AI features for unsupported browsers.
Output: Modified next.config.mjs with headers, new WebGPUFallback component, tests for both.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-webllm-foundation/RESEARCH.md

@next.config.mjs
@__tests__/hooks/use-pyodide-worker.test.ts
</context>

<interfaces>
<!-- Existing next.config.mjs headers() structure — insertion point for COEP/COOP -->
From next.config.mjs:
```javascript
async headers() {
  return [
    { source: '/:path*.json', headers: [{ key: 'Cache-Control', value: '...' }] },
    { source: '/workers/:path*', headers: [{ key: 'Cache-Control', value: '...' }] },
    { source: '/data/:path*', headers: [{ key: 'Cache-Control', value: '...' }] },
  ]
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: COEP/COOP headers in next.config.mjs + header config test</name>
  <files>next.config.mjs, __tests__/lib/next-config-headers.test.ts</files>
  <behavior>
    - Test 1: headers() return array contains a rule with source '/(.*)'
    - Test 2: that rule includes header Cross-Origin-Embedder-Policy with value 'credentialless'
    - Test 3: that rule includes header Cross-Origin-Opener-Policy with value 'same-origin'
    - Test 4: the COEP/COOP rule is the FIRST element in the returned array (index 0)
  </behavior>
  <action>
    1. Create `__tests__/lib/next-config-headers.test.ts`:
       - Import the nextConfig from next.config.mjs (the raw config object before withMDX wrapping). Since next.config.mjs exports `withMDX(nextConfig)` which is not directly testable for headers, instead test by calling the headers function. Import the default export, and call its `headers()` method (Next.js config objects have headers as an async function).
       - Actually, since next.config.mjs exports the MDX-wrapped config, and the headers() function is preserved through wrapping, import the default export and call `.headers()` on it. The function returns a promise of the headers array.
       - Write tests asserting the COEP/COOP rule exists at index 0, with correct source and header values.

    2. Modify `next.config.mjs`:
       - Add a new rule as the FIRST element in the `async headers()` return array:
         ```javascript
         {
           source: '/(.*)',
           headers: [
             { key: 'Cross-Origin-Embedder-Policy', value: 'credentialless' },
             { key: 'Cross-Origin-Opener-Policy', value: 'same-origin' },
           ],
         },
         ```
       - Use `credentialless` (NOT `require-corp`) to avoid breaking NotebookLM deeplinks per user decision.
       - Place BEFORE existing cache rules so specific path rules are not shadowed.
  </action>
  <verify>
    <automated>pnpm test -- next-config-headers --reporter=verbose --run</automated>
  </verify>
  <done>COEP/COOP headers configured at index 0 in headers() array. 4 tests pass validating header presence, values, and ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebGPUFallback component with NotebookLM link</name>
  <files>components/webgpu-fallback.tsx, __tests__/components/webgpu-fallback.test.tsx</files>
  <behavior>
    - Test 1: WebGPUFallback renders "In-browser AI not available" heading text
    - Test 2: WebGPUFallback renders an anchor linking to the provided notebookLmUrl
    - Test 3: The anchor has target="_blank" and rel="noopener noreferrer"
    - Test 4: The anchor text contains "NotebookLM"
  </behavior>
  <action>
    1. Create `__tests__/components/webgpu-fallback.test.tsx`:
       - Import render and screen from @testing-library/react
       - Import WebGPUFallback from @/components/webgpu-fallback
       - Test the 4 behaviors above using a test notebookLmUrl like 'https://notebooklm.google.com/test'

    2. Create `components/webgpu-fallback.tsx`:
       - 'use client' directive
       - Export function `WebGPUFallback({ notebookLmUrl }: { notebookLmUrl: string })`
       - Render a styled div with:
         - Heading: "In-browser AI not available" (font-medium text-foreground)
         - Body: "Your browser does not support WebGPU. Use NotebookLM for AI assistance with this course."
         - "NotebookLM" is an anchor tag to notebookLmUrl with target="_blank" and rel="noopener noreferrer"
       - Use Tailwind classes consistent with existing components: rounded-lg border p-4 text-sm text-muted-foreground
  </action>
  <verify>
    <automated>pnpm test -- webgpu-fallback --reporter=verbose --run</automated>
  </verify>
  <done>WebGPUFallback component renders fallback message with NotebookLM link. 4 tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm test -- next-config-headers --reporter=verbose --run` — all header tests pass
- `pnpm test -- webgpu-fallback --reporter=verbose --run` — all fallback component tests pass
- `pnpm build` — SSG build still succeeds (no runtime-only code in config)
- `pnpm test --run` — full test suite passes (no regressions)
</verification>

<success_criteria>
- next.config.mjs headers() returns COEP credentialless + COOP same-origin as first rule
- WebGPUFallback component renders NotebookLM link
- All new tests pass, full suite green, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-webllm-foundation/13-01-SUMMARY.md`
</output>
